{
  "name": "@aqua/plugin-notifications",
  "version": "0.1.0",
  "private": true,
  "description": "Aqua portal notification routing — fans foundation activity events out to per-user channel preferences (email · slack · whatsapp-stub · generic webhook). Pluggable channel-driver ports; per-(userId, eventId) cooldown dedup; graceful when paired plugins (activity-inbox · email-sender) are absent.",
  "type": "module",
  "main": "index.ts",
  "exports": {
    ".": "./index.ts",
    "./manifest": "./index.ts",
    "./server": "./src/server/index.ts",
    "./types": "./src/lib/aquaPluginTypes.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "smoke": "tsx --test src/__smoke__/notifications.test.ts",
    "test": "tsx --test src/__smoke__/notifications.test.ts"
  },
  "peerDependencies": {
    "next": "^16.0.0",
    "react": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
