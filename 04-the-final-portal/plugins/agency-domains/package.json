{
  "name": "@aqua/plugin-agency-domains",
  "version": "0.1.0",
  "private": true,
  "description": "Aqua portal agency-side custom-domain attach skeleton — records intent + status (pending|verifying|active|failed) + NS records the client needs to set on their registrar. Real DNS provisioning + TLS issuance deferred to T6; existing @aqua/plugin-domains is the production-wired sibling.",
  "type": "module",
  "main": "index.ts",
  "exports": {
    ".": "./index.ts",
    "./manifest": "./index.ts",
    "./server": "./src/server/index.ts",
    "./types": "./src/lib/aquaPluginTypes.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "smoke": "tsx --test src/__smoke__/agency-domains.test.ts",
    "test": "tsx --test src/__smoke__/agency-domains.test.ts"
  },
  "peerDependencies": { "next": "^16.0.0", "react": "^19.0.0" },
  "devDependencies": {
    "@types/node": "^22.0.0", "@types/react": "^19.0.0",
    "tsx": "^4.21.0", "typescript": "^5.6.0"
  },
  "engines": { "node": ">=20.0.0" }
}
