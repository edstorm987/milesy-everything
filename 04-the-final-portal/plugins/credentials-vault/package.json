{
  "name": "@aqua/plugin-credentials-vault",
  "version": "0.1.0",
  "private": true,
  "description": "Aqua portal credentials vault — per-client (or agency-wide) login info, API keys, 2FA recovery codes, and access notes. AES-256-GCM at rest; reveal-password is rate-limited and activity-logged. v1 admin/staff visibility with per-credential sharedWith ACL.",
  "type": "module",
  "main": "index.ts",
  "exports": {
    ".": "./index.ts",
    "./manifest": "./index.ts",
    "./server": "./src/server/index.ts",
    "./types": "./src/lib/aquaPluginTypes.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "smoke": "tsx --test src/__smoke__/vault.test.ts",
    "test": "tsx --test src/__smoke__/vault.test.ts"
  },
  "peerDependencies": {
    "next": "^16.0.0",
    "react": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
