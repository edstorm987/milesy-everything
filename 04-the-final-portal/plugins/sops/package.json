{
  "name": "@aqua/plugin-sops",
  "version": "0.1.0",
  "private": true,
  "description": "Aqua portal SOPs / Docs / Templates shelf — agency-scope notes plugin gated by 5 tag families (sales / service / leads / standards / mastery). Markdown bodies; per-install storage; v1 admin/viewer permissions, Employee HQ swaps in `sops.tag.<family>` keys later.",
  "type": "module",
  "main": "index.ts",
  "exports": {
    ".": "./index.ts",
    "./manifest": "./index.ts",
    "./server": "./src/server/index.ts",
    "./types": "./src/lib/aquaPluginTypes.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "smoke": "tsx --test src/__smoke__/sops.test.ts",
    "test": "tsx --test src/__smoke__/sops.test.ts"
  },
  "peerDependencies": {
    "next": "^16.0.0",
    "react": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
