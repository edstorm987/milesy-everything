{
  "name": "@aqua/plugin-stripe-events",
  "version": "0.1.0",
  "private": true,
  "description": "Aqua portal Stripe-events plumbing — webhook ingestion + idempotent event log + subscription state mirror. NO charges / money flow in v1; just `we can see Stripe events for a connected tenant`.",
  "type": "module",
  "main": "index.ts",
  "exports": {
    ".": "./index.ts",
    "./manifest": "./index.ts",
    "./server": "./src/server/index.ts",
    "./types": "./src/lib/aquaPluginTypes.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "smoke": "tsx --test src/__smoke__/stripe.test.ts",
    "test": "tsx --test src/__smoke__/stripe.test.ts"
  },
  "peerDependencies": { "next": "^16.0.0", "react": "^19.0.0" },
  "devDependencies": {
    "@types/node": "^22.0.0", "@types/react": "^19.0.0",
    "tsx": "^4.21.0", "typescript": "^5.6.0"
  },
  "engines": { "node": ">=20.0.0" }
}
