{
  "name": "@aqua/plugin-bookings",
  "version": "0.1.0",
  "private": true,
  "description": "Aqua portal bookings — calendar-backed appointments for therapists. Per-client services, weekly availability + exceptions, slot generation respecting buffers + capacity, ICS confirmation emails. Pairs with @aqua/plugin-client-crm (auto-create contact on completed booking) and @aqua/plugin-email-sender (confirmation send) when present.",
  "type": "module",
  "main": "index.ts",
  "exports": {
    ".": "./index.ts",
    "./manifest": "./index.ts",
    "./server": "./src/server/index.ts",
    "./types": "./src/lib/aquaPluginTypes.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "smoke": "tsx --test src/__smoke__/bookings.test.ts",
    "test": "tsx --test src/__smoke__/bookings.test.ts"
  },
  "peerDependencies": {
    "next": "^16.0.0",
    "react": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
