# T1 — Foundation log

Append-only. Format: `[ISO timestamp] TYPE: message` (see `messages/README.md`).

[2026-05-03T23:49:06Z] STARTED: T1 foundation. Pre-reads done (CLAUDE.md, messages/README, MASTER, 04-architecture, plugin-system, server-modules, auth-middleware, roles-tenancy, eds requirments). Plan: lift 02 plugin runtime + storage/eventBus/orgs/users/lib-auth, adapt for three-level tenancy (agencyId/clientId), build server-rendered chrome, scaffold next.js 16 app, ship working /,/login,/embed/login,/portal/agency. Commit per sub-task.
[2026-05-03T23:55:06Z] PROGRESS: package.json/tsconfig/next.config/tailwind/postcss/.env.example/.gitignore in place under 04-the-final-portal/portal/. npm install clean (next 16.2.4, react 19.2.4, tailwind 4). Server modules written: types.ts (Agency/Client/EndCustomer/Role/SessionPayload/PluginInstall/PortalState), storage.ts (file backend, async hydrate, sync read, debounced flush — KV/Postgres stubbed), eventBus.ts (typed pub/sub), tenants.ts (three-level CRUD with strict agency-scope filtering — no global "list every client"), users.ts (scrypt + role hierarchy lifted from 02), pluginInstalls.ts (composite-key install records, agency-wide OR client-scoped), activity.ts, phases.ts (read-only stub for T2). Q-ASSUMED: PluginInstall scope encoded as `${agencyId}|${clientId ?? "_agency"}|${pluginId}` — gives one row per install regardless of agency-vs-client scope, simplifies storage and uninstall. Continuing with auth + middleware + chrome + plugin runtime + pages.
[2026-05-04T07:05:00Z] PROGRESS: auth + chrome + plugin runtime + pages all in. lib/server/auth.ts (HMAC cookie, getSession, requireRole, requireRoleForClient, AuthError). proxy.ts (Next 16's middleware → proxy rename) gates /portal/* and best-effort tenant-scope match. lib/chrome/{sidebarLayout,brandKit}.ts + components/chrome/{Sidebar,Topbar,ThemeInjector}.tsx — server-rendered, NavPanel-driven, role-aware default contributions, plugin items merged by panelId with `:clientId` href rewriting. Plugins runtime adapted to scope `{agencyId, clientId?}` with required `scopePolicy: "client"|"agency"|"either"` field on every manifest (Q-ASSUMED, deviation logged). Pages: /, /login, /embed/login (?client=<id> for branding), /portal (role-aware redirect), /portal/agency (AGENCY_ROLES gate, agency brand), /portal/clients (agency-side list), /portal/clients/[clientId] (ALL_ROLES + tenant scope match, client brand), /portal/customer. API: /api/auth/{login,logout,me} + /api/tenants/seed (dev). Login bootstraps Milesy Media + agency-owner on first run when state empty.
[2026-05-04T07:05:30Z] PROGRESS: smoke-tested. `npm run build` clean (no warnings). `npx tsc --noEmit` clean. Boot sequence: HEAD / 200, /login 200, /embed/login 200, /portal 307→/login (no cookie), POST /api/auth/login bootstrap → 200 with cookie, GET /portal/agency 200 (chrome rendered, sidebar built, brand-kit injected). Chronic dev-server cache-bug noted: under Turbopack the very-first /portal/* request hydrates state to empty before bootstrap fires; subsequent requests work — no impact on prod.
[2026-05-04T07:07:55Z] DONE: foundation scaffold complete. Chapter 21 added at `01 development/context/prior research/04-foundation.md` (file tree, plugin contract surface, auth API, chrome contract, server module surface, bootstrap, 6 deviations from architecture). MASTER.md row 21 added. tasks.md row moved to "Done — Round 1". Ready for T2/T3 to land their plugin manifests by importing into `04-the-final-portal/portal/src/plugins/_registry.ts`.

[2026-05-04T07:17:15Z] STARTED: Round 2 wire-and-demo. Pre-reads done (CLAUDE, MASTER, 04-architecture, 04-foundation, 04-plugin-fulfillment, fulfillment manifest + ports + container). Found contract drift: T2's local `aquaPluginTypes.ts` has richer PluginCtx (services, actor) + PluginPageProps (services, actor, searchParams, storage) + NavItem (visibleToRoles, parent) than T1's R1 _types.ts; also no `scopePolicy` field. Plan: align T1's _types.ts with T2's surface (T2 is the more complete contract — T1 R1 was incomplete on services injection), then ship workspace dep + adapters + catch-all routes + demo seed. Q-ASSUMED: I'll keep `scopePolicy` as an optional field defaulting to "either" so T2's manifest validates. Q-ASSUMED: I'll make T1's NavItem accept BOTH `roles` (R1 alias) and `visibleToRoles` (T2's name) — preferred is `visibleToRoles`. Continuing.

[2026-05-04T07:32:00Z] PROGRESS: contract aligned (T1 _types.ts now matches T2's `aquaPluginTypes.ts` surface — services + actor on PluginCtx, services + actor + searchParams + storage on PluginPageProps, visibleToRoles + parent on NavItem, scopePolicy optional). 8 foundation adapters under src/plugins/foundation-adapters/ (clientStore / pluginInstalls / pluginRuntime / pluginRegistry / phaseStore / activityLog / eventBus / portalVariant STUB). FOUNDATION_SERVICES singleton wired through _runtime.ts ctx. _routeResolver.ts resolves agency/client URLs → plugin manifest pages. 3 catch-all routes: /portal/agency/[...rest]/page.tsx, /portal/clients/[clientId]/[...rest]/page.tsx, /api/portal/[plugin]/[...rest]/route.ts. agencyBootstrap.ts wraps createAgency + installCorePluginsForScope so fulfillment.onInstall fires (seeds 6 phase defaults) on every new agency. /api/dev/seed-demo endpoint creates Demo · Aqua + Luv & Ker · Demo (orange/cream/Playfair) at onboarding stage with half-ticked checklist. Workspace dep wiring needed `install-links=true` (.npmrc) + `transpilePackages` in next.config.ts — Turbopack 16 doesn't follow npm symlinks for file: deps.
[2026-05-04T07:33:00Z] PROGRESS: smoke pass end-to-end. POST /api/dev/seed-demo → 200 with credentials. login → 200, cookie set. /portal/agency 200, /portal/agency/fulfillment 200 (catch-all → ClientsPage), /portal/agency/fulfillment/clients 200, /portal/agency/fulfillment/<clientId> 200 (PhaseBoardPage), /portal/agency/fulfillment/phases 200, /portal/agency/fulfillment/marketplace?client=<id> 200. API: /api/portal/fulfillment/clients returns Felicia. /api/portal/fulfillment/phases returns 6 seeded phases. /api/portal/fulfillment/checklist?clientId&phaseId returns ChecklistView (2/3 internal done). POST /api/portal/fulfillment/checklist/tick records the tick + activity log entry. npm run build clean (no warnings). tsc --noEmit clean across portal + fulfillment.
[2026-05-04T07:35:07Z] DONE: Round 2 wire-and-demo complete. Chapter 23 added at `01 development/context/prior research/04-foundation-round2.md` (workspace dep wiring, port adapters, catch-all resolver, demo seed shape, smoke results, T3 handoff notes, deviations from R1 contract). MASTER.md row 23 added. tasks.md "Done — Round 2" section added; up-next reflects T1 R3 (real portalVariantAdapter once T3 R2 lands as workspace dep).

[2026-05-04T07:50:00Z] STARTED: Round 3 wake — three-plugins-live. Pulled latest (T2 R2 ecommerce shipped at 4f6b264). Pre-reads done. Plan: extend route resolver for T3's full-URL manifest paths, wire ecommerce + website-editor as workspace deps, replace portalVariantAdapter stub with real T3 wrapper, register ecommerce foundation at boot, extend demo seed to install client-scoped plugins, smoke all three.
[2026-05-04T07:55:00Z] WARN: T2's ecommerce manifest depends on `registerEcommerceFoundation()` running at boot, but the function isn't exported from `@aqua/plugin-ecommerce/server` (it lives in `./foundationAdapter`, and the package's `exports` map blocks deeper imports). Per Round-2-prompt's cross-team-patch authorisation, I added a one-line re-export to `plugins/ecommerce/src/server/index.ts`. Notified T2 in their inbox. Smoke now green.
[2026-05-04T08:14:38Z] DONE: Round 3 wire-three-plugins complete. Chapter 25 added at `01 development/context/prior research/04-foundation-round3.md`. MASTER.md row 25 added. tasks.md "Done — Round 3" section. Smoke verified: 14 page URLs all 200 across all three plugin URL families, fulfillment+ecommerce APIs dispatch, T3 sites endpoint returns ok. Real portalVariantAdapter wired (T3's applyStarterVariant bound to website-editor's plugin storage). Ecommerce foundation registers at boot via side-effect import. Demo seed installs website-editor + ecommerce on the Felicia client. tsc + build clean.

[2026-05-04T10:33:00Z] PROGRESS: 3 consecutive empty wakes after R3 DONE. Per loop discipline, ending the autonomous loop. R1 + R2 + R3 all shipped (commits 16bc524, f02863a, 29bd49a). Foundation hosts three plugins live; smoke green. Awaiting Round 4 prompt or commander direction; Ed re-pastes when ready.

[2026-05-04T20:14:00Z] STARTED: Round 4 demo button. Pre-reads done (CLAUDE, messages README, MASTER, 04-architecture §8 + §11, 04-foundation-round3, eds requirments, static site index/login/admin/styles + README, portal auth/seed/login/layouts). Plan: A) static site → add data-portal-base meta + inline rewriter for Sign in + new Demo CTA + README env doc; B) extend SessionPayload with isDemo, factor seed body into a shared module so /portal/demo can call it without going through the API gate, build /portal/demo + /portal/demo/toggle + DemoBanner component injected at /portal layout; C) extend /api/dev/seed-demo to support ?reset=1 by wiping demo agency + all children + re-seeding via the shared module; D) replace existing footer "build YYYY-MM-DD" string with "Last deployed YYYY-MM-DD" + document bump-on-deploy convention. Q-ASSUMED: SessionPayload.isDemo is the durable marker (lives inside the HMAC-signed cookie) — sandboxed POV is per-session, not per-user. Continuing.

[2026-05-04T20:25:00Z] Q-ASSUMED: prompt names the demo route `/portal/demo/page.tsx`, but `/portal` layout requires a session and would redirect anonymous demo visitors to /login. Architecture §11 lists `milesymedia.com/demo` at the top level. Mounting the demo route at `src/app/demo/` (URL `/demo`) and pointing the static site's Demo CTA at `${base}/demo?source=milesymedia` keeps the visitor outside the portal session gate until the cookie is issued, then internally redirects to `/portal/agency`. Continuing on this shape.

[2026-05-04T20:30:00Z] COMMIT: 6c937a9 "T1 R4 step 1: wire static site Sign in + Demo to portal". Goal A + D done — static-site Sign in + new Demo CTA hand off via meta-tag-configurable portal base; index + login footers carry "Last deployed YYYY-MM-DD"; README documents the bump-on-deploy convention. Moving to Goal B (portal /demo flow + isDemo session + POV toggle banner).

[2026-05-04T21:05:00Z] COMMIT: cb68287 "T1 R4 step 2: /demo flow + isDemo cookie + POV toggle + reset". Goals B + C done. SessionPayload gained optional isDemo. New top-level routes /demo + /demo/toggle (Route Handlers, not /portal/demo — the /portal layout's session gate would block anonymous visitors; logged Q-ASSUMED 20:25). Seed body factored into src/lib/server/demoSeed.ts; resetDemo() wipes the demo tenant + every descendant. /api/dev/seed-demo?reset=1 gated reset. DemoBanner server component injected at /portal/layout.tsx — sticky amber strip across agency + client surfaces with Switch + Leave actions. Smoke verified end-to-end: /demo cold → 307 + isDemo cookie → /portal/agency 200 + banner; toggle a→c → /portal/clients/<id> with new cookie; toggle c→a back; POST /api/dev/seed-demo idempotent; ?reset=1 wipes 1/1/2/3/2/6/7/0 and re-seeds clean; toggle without demo cookie bounces to /demo; real /api/auth/login still works and produces NO banner. tsc + build clean.

[2026-05-04T21:15:00Z] DONE: T1 R4 complete. Chapter 27 added at `01 development/context/prior research/04-milesymedia-demo.md` (static-site rewriter pattern, isDemo cookie shape, route placement rationale, shared seed module, reset semantics, banner injection point, R4 deviations, smoke results, cross-team handoff). MASTER.md row 27 added under "Round 4 implementation chapters". tasks.md "Done — Round 4" section added; up-next block reflects post-R4 backlog (T3 R2 block UIs, Stripe webhook smoke, first phase preset E2E, nightly demo cron). Final commit pending after this entry.

[2026-05-04T21:20:00Z] COMMIT: 322ef74 "T1 R4 step 3: chapter + MASTER + tasks — DONE". Three commits this round (6c937a9 static-site, cb68287 portal demo flow, 322ef74 chapter+MASTER+tasks). All pushed to origin/main. R4 closed.

[2026-05-05T08:30:00Z] STARTED: Round 5 end-customer flow. Pre-reads done (CLAUDE, MASTER, 04-architecture §1/§3/§11, 04-foundation R1/R2/R3, 04-milesymedia-demo R4, aqua-portal-variants, 04-plugin-website-editor §applyStarterVariant + §portalVariants, eds requirments three-audiences). Read current portal/customer layout/page, embed/login, /api/auth/login, users.ts, tenants.ts, types.ts, _routeResolver, sidebarLayout, portalVariantAdapter, T3's getActivePortalVariant + EditorPage shape. Plan: A) POST /api/auth/end-customer/signup with rate limits + (agencyId,clientId)-scoped email uniqueness — adapt users.ts to add scoped lookup helper, leave existing verifyPassword(email) alone; B) replace /portal/customer/page.tsx with variant-driven server component (call T3's getActivePortalVariant via foundation services for the active "account" variant, fallback "login", final fallback welcome card + customer-panel plugin links); add /portal/customer/[...rest] catch-all sharing _routeResolver; verify buildSidebar scope:"customer" filters by panelId; C) extend seedDemoAgency to seed demo-shopper@aqua.test scoped to Felicia mirror, surface creds, cycle /demo/toggle agency→client→customer→agency, DemoBanner shows three-state POV + "Next view"; resetDemo wipes the customer too (already does — covered by users-by-agencyId loop); D) embed LoginForm carries clientId in POST body + signup toggle when signupsEnabled, success → parent.location.href = ?return || `${origin}/portal/customer`. Q-ASSUMED: scoped email lookup via new getUserByEmailScoped(email, agencyId, clientId?) — keep state.users keyed by lower email but allow multiple records by suffixing the key with `|c:<clientId>` for end-customers. Continuing.

[2026-05-05T09:45:00Z] COMMIT: f613b17 "T1 R5 step 1: end-customer flow — signup, login, /portal/customer, 3-POV demo". Goals A–D shipped: scoped user storage key (`email|c:<clientId>`) + `UserLookupScope`; `POST /api/auth/end-customer/signup` (rate-limited, signupsEnabled gate, 409 dup); `/api/auth/login` accepts embed clientId; `Client.endCustomers` config; variant-driven `/portal/customer` calling T3's `getActivePortalVariant` (account → login fallback) + customer route family + `panelId:"customer"` sidebar scope; demo seed adds `demo-shopper@aqua.test`, `/demo/toggle` 3-POV cycle, DemoBanner three-state labels; embed LoginForm signup toggle + parent-frame return URL. Smoke: signup → 200 + cookie, /portal/customer 200 with brand applied, dup signup → 409, embed `?client=` resolves brand title (Turbopack stale-worker quirk required dev restart per R1 chronic note + R2 R3 install-link refresh tip), full POV cycle agency→client→customer→agency all 200, reset wipes 5 users + re-seeds with `customer:true`. tsc + build clean.

[2026-05-05T09:55:00Z] DONE: T1 R5 complete. Chapter 35 added at `01 development/context/prior research/04-end-customer-flow.md` (scoped user storage key rationale, signup pipeline, login extension, variant-driven /portal/customer with fallback hierarchy, customer route family + ambiguity guard, panelId:"customer" sidebar scope, three-state demo POV cycle, embed bridge return-URL precedence, R5 deviations, smoke results, cross-team handoff notes for `Client.endCustomers` field shape + getActivePortalVariant import surface + install-link refresh + first-request hydrate quirk). MASTER.md row 35 added (correctly numbered above T3's row 34 to keep round-grouped order). tasks.md "Done — Round 5" gains the T1 row at the top of the section; In-Progress's T1 R5 line replaced with a `_(done — see below)_` pointer. Final commit pending.

[2026-05-05T10:00:00Z] COMMIT: 8649187 "T1 R5 step 2: chapter + MASTER + tasks — DONE". Two commits this round (f613b17 portal end-customer flow, 8649187 chapter+MASTER+tasks). Both pushed to origin/main. R5 closed.

[2026-05-05T10:30:00Z] STARTED: Round 6 foundation mass plugin wire-up + event router. Pre-reads done (CLAUDE, MASTER, 04-architecture §2/§6/§7, 04-foundation R1/R2/R3, 04-end-customer-flow R5, plugin chapters 28/30/31/32/33/34, each plugin's server/index + foundationAdapter + ports). Plan: A) 6 file:.. deps + 9 transpilePackages; B) shared `_foundationPorts.ts` (tenant/activity/events/pluginInstalls/user) + `_crossPluginPorts.ts` (ecommerceOrders projections + membershipBenefits) + 6 side-effect-import files + _registry append; C) ActivityCategory += hr/memberships/affiliates/finance/marketing/crm; D) eventBus.subscribeForPlugin + tenant-filtered fan-out + `_eventSubscribers.ts` for affiliates/client-crm wires; E) demoSeed installs 5 client + 3 agency plugins; F) scripts/smoke.mjs harness + chapter. Q-ASSUMED: each register*Foundation call site casts to `unknown as Parameters<...>` to bridge plugin-vendored ActivityCategory drift (each plugin's vendored union is narrower than foundation's superset — runtime data flows correctly, TS structural check needs the cast). Q-ASSUMED: membershipsFoundation.stripeFor returns a NOOP StripePort instead of null so containerFor builds (paid flows throw with clear errors); a real Stripe SDK adapter is foundation-pending. Q-ASSUMED: sidebarLayout surfaces "discovered" panels (panelIds not in DEFAULT_PANELS) between Tools/Settings so plugins ship new panel ids without a foundation patch.

[2026-05-05T11:30:00Z] COMMIT: e297f5d "T1 R6 step 1: foundation mass plugin wire-up + cross-plugin event router". 21 files / +1035 LOC. Goals A-F shipped. Smoke 35/35 pass: /demo cold, all 9 install entries present, 11 nav URLs 200, 6 API surfaces 200 (memberships/affiliates/client-crm need ?clientId= since they're client-scoped), full POV cycle agency→client→customer→agency. tsc + build clean. Discovered an interesting drift: `client-crm` declares panelId="growth" + category="growth" — needed widening in `_validate.ts` AND a discovered-panels render path in sidebarLayout. Documented for the chapter.

[2026-05-05T11:50:00Z] DONE: T1 R6 complete. Chapter 39 added at `01 development/context/prior research/04-foundation-round6.md` (R6 plugin matrix with scopes, replicate-for-future-plugin wire-up checklist, shared port objects, cross-plugin port adapters, subscribeForPlugin contract + two-stage fan-out, plugin-vendored type drift workaround, validator + sidebar widening, memberships Stripe stub, demo seed shape after R6, smoke harness, R6 deviations + open follow-ups, cross-team handoff notes). MASTER.md row 39 added under Round 4 chapter stack (next free number — round-grouped order is preserved by chapter id, the R-tag in the title indicates round). tasks.md "Done — Round 6" gains the T1 row above T2 R6; In-Progress's T1 R6 line replaced with `_(done — see below)_` pointer. Final commit pending.

[2026-05-05T11:55:00Z] COMMIT: ae83df0 "T1 R6 step 2: chapter + MASTER + tasks — DONE". Two commits this round (e297f5d portal wire-up + event router, ae83df0 chapter+MASTER+tasks). Both pushed to origin/main. R6 closed.

[2026-05-05T12:25:00Z] PROGRESS: /loop re-fired the R6 prompt; R6 is already shipped on origin/main (e297f5d / ae83df0 / 4373d39). Inbox shows commander's R7 TASK entry (Postgres backend, prompt at `01 development/terminal-prompts/T1-round7-postgres-backend.md`). R7 is a different round with its own goals (Postgres driver behind storage abstraction, migration script, connection pooling, per-tenant scoping, both-backends smoke + chapter `04-foundation-round7-postgres.md`). Ending the R6 /loop here — won't silently switch loops to R7. Awaiting user /loop with the R7 prompt to start the next round.

[2026-05-05T12:35:00Z] STARTED: Round 7 Postgres backend. Pre-reads done (CLAUDE, MASTER, 04-architecture §6/§13, 04-foundation R1 §"Storage abstraction", aqua-server-modules §"Storage abstraction" reference, R6 chapter, current `src/server/storage.ts`, every plugin's *Foundation.ts to check storage consumers). Plan: A) `pg` dep + new `postgresBackend` slotted into the existing `Backend` interface (loadBlob/saveBlob single-row JSONB at key `__portal_state__` — keeps the snapshot-write-flush pattern intact, no consumer refactor needed; the `key/value/updated_at` schema is shaped for future per-key expansion); B) `scripts/schema.sql` + `scripts/migrate-file-to-postgres.mjs` reading `.data/portal-state.json` and idempotent-upserting via ON CONFLICT; C) `pg.Pool` from DATABASE_URL, default file backend when unset (so dev still works); D) RLS deferred — single-blob row gates the row, not in-blob fields; existing `withTenantScope` in tenants.ts is the operating defense; document as R8 hardening; E) extend `scripts/smoke.mjs` to honour STORAGE_BACKEND env, run file-backend smoke green, document the postgres runbook for environments with a real DB. Q-ASSUMED: blob-row pattern over per-key rows for v1 — every consumer reads from the in-memory snapshot; a per-key Postgres layout would require refactoring `getState()` callers. Logged as Goal-C Q-ASSUMED in the chapter.

[2026-05-05T13:30:00Z] PROGRESS: Goals A-D shipped. `pg`+`@types/pg` deps; `src/server/storagePostgres.ts` lazy Pool with TLS auto-detect; `storage.ts` `postgresBackend` via dynamic import + implicit promotion when DATABASE_URL set. `scripts/schema.sql` (portal_kv + text_pattern_ops btree); `scripts/migrate-file-to-postgres.mjs` idempotent + DRY_RUN; `scripts/smoke-postgres.mjs` (8 checks). End-to-end verified against a local Postgres (`eds@localhost/aqua_portal_r7_smoke`): schema applied, migration moved 24KB blob from `.data/portal-state.json` to `portal_kv`, all 8 direct smoke checks pass. RLS deferred to R8 (documented). Next-hosted HTTP smoke against postgres blocked by parallel-session Next single-instance dev lock — direct smoke covers the driver. tsc clean. `npm run smoke:postgres` + `npm run migrate:file-to-postgres` aliases added.

[2026-05-05T13:35:00Z] WARN: Parallel session in same repo (likely T2 or commander) holds Next dev's single-instance lock on PID changes constantly. My 3050-bound dev keeps starting then exiting on the lock check. Worked around with `scripts/smoke-postgres.mjs` (no Next required). Documented as a known limitation in the R7 chapter; resolves naturally when the other session releases the lock.

[2026-05-05T13:40:00Z] DONE: T1 R7 complete. Chapter 42 added at `01 development/context/prior research/04-foundation-round7-postgres.md` (schema rationale, driver shape, snapshot Q-ASSUMED, migration runbook, RLS deferral reasoning, smoke results, production deployment checklist for Neon/Supabase/Vercel-Postgres/self-hosted, R7 deviations + open follow-ups). MASTER.md row 42 added (T2 R10 took 41 between my chapter draft and final stage). tasks.md "Done — Round 7" gains the T1 row above T2 R7. Final commit pending.

[2026-05-05T13:50:00Z] WARN: Parallel-session race condition — T6's commit `05dea79` ("T6 R1 Phase A: Vercel monorepo project config + deploy orchestrator") bundled all 10 of my staged R7 files (storagePostgres.ts, storage.ts, scripts/schema.sql, scripts/migrate-file-to-postgres.mjs, scripts/smoke-postgres.mjs, env.example, package.json + package-lock, the chapter, my outbox entry) under T6's commit message. T6 ran `git add` + `git commit` while my files were staged. Code is preserved on origin/main; commit attribution is misleading. MASTER.md + tasks.md R7 rows landed in a separate commit. R7 work is fully shipped — refer to `05dea79` for the actual code, chapter 42 (`04-foundation-round7-postgres.md`) for the architecture write-up. Smoke 8/8 pass via `npm run smoke:postgres`.

[2026-05-05T14:10:00Z] PROGRESS: wake 1/3 empty post-R7 DONE. Inbox carries commander REPLY 12:40Z approving the blob-row Q-ASSUMED (chapter §3 already documents v1 limits + per-key v2 path), and a PLAN 12:40Z heads-up that an R8 (`T1-round8-milesymedia-portal-stitch.md`) is queued — not yet TASKed to my inbox. tasks.md "In progress" carries no T1 entry. Architecture extension chapter 19b (`04-architecture-extension-per-client-portals.md`) is the new operating context (milesymedia.com front door + Aqua portal at `/portal` + per-Live-client `clients/<slug>/` apps). Will pre-read it when R8 lands. Scheduling next wake.

[2026-05-05T14:35:00Z] PROGRESS: /loop re-fired the R7 prompt; R7 is shipped on origin/main (chapter 42, commits 05dea79 + sibling docs). Inbox now carries the R8 TASK entry (`13:30Z TASK: Round 8 prompt at terminal-prompts/T1-round8-milesymedia-portal-stitch.md` — three goals: A) local dev rewrites/proxy, B) Vercel `vercel.json` updates, C) chapter notes appended to R4 demo + R5 customer flow). Coordination required with T6's R1 Phase A (Vercel monorepo project config, commit `359b476`). R8 is a different prompt with its own goals — won't silently switch loops. Ending the R7 /loop here. Awaiting user /loop with the R8 prompt to start the next round.

[2026-05-05T15:00:00Z] STARTED: Round 8 milesymedia ↔ portal stitch (autonomous loop firing post-end-of-R7-loop). Pre-reads queued: CLAUDE, MASTER, 04-architecture §11 (URL surface), 04-architecture-extension-per-client-portals.md (chapter 19b — the new operating context), 04-milesymedia-demo R4, 04-end-customer-flow R5, milesymedia website HTMLs, current portal next.config.ts + root vercel.json. Plan: A) local-dev — Next.js rewrites pointing fallthrough at a static-server side car (cleaner than a custom proxy; Next 16 supports `rewrites().fallback` shape). `npm run dev:all` via concurrently. T6's monorepo Vercel work (commit 359b476) is the bedrock for B). B) Update root vercel.json — switch output to portal Next.js build + add path rewrites for /, /index.html, /admin.html, /login.html, /styles.css → static milesymedia. Static site `data-portal-base` default switches to empty = same-origin. C) Chapter notes appended to R4 + R5 chapters; new R8 chapter `04-milesymedia-portal-stitch.md`. Q-ASSUMED: Next.js rewrites approach over a custom dev-proxy — simpler, no extra script to maintain, single port.

[2026-05-05T15:55:00Z] PROGRESS: Goals A + B + C all shipped. Goal B was a nice surprise: T6 R1 Phase A's commit `359b476` already shipped the production-side root vercel.json + scripts/build-portal.mjs (rewrites for /, /index.html, /admin.html, /login.html, /styles.css → /_milesy/<file>; portal/.next as outputDirectory; static-site copy step inside the build). My R8 work focused on making dev parity match: new `portal/scripts/prepare-milesy.mjs` (idempotent copy step shared between predev + prebuild — single canonical implementation), `next.config.ts.rewrites().beforeFiles` mirroring the production rewrites table 1:1, `npm run dev:all` alias. Q-ASSUMED logged: chose single-port + Next.js rewrites over the prompt's two-server `concurrently` pattern (simpler, no extra script, no config drift). Static-site meta `data-portal-base` defaulted to "" (same-origin) across all 3 HTMLs; `?portalBase=…` query override added for standalone-preview workflows. `public/_milesy/` ignored. Smoke green end-to-end against `localhost:3050`: `/` 200 (milesymedia landing renders with hero copy + Try-the-live-demo CTA), `/styles.css` 200 text/css, `/login` 200 (Next.js portal login surface), `/login.html` 200 (static login mock — distinct surface), `/admin.html` 200 (static admin mock), `/demo?source=stitch-smoke` 307 + isDemo cookie + redirect to /portal/agency. tsc clean.

[2026-05-05T16:05:00Z] DONE: T1 R8 complete. Chapter 48 added at `01 development/context/prior research/04-milesymedia-portal-stitch.md` (one-origin/two-folder discipline, prepare-milesy + next.config rewrites + npm scripts, production root vercel.json reference, same-origin cookie surface, static-site `data-portal-base=""` default, files-separate puzzle-piece discipline, smoke results, R8 deviations + open follow-ups, cross-team handoff notes for T6 R2 / T2 export-to-repo / T3 editor / demo cron). MASTER.md row 48 added (next free integer; 43-47 + 44 already taken). tasks.md "Done — Round 8" gains the T1 row above T2 R8 client-CRM. R4 demo + R5 end-customer chapters appended with "Round 8 update" sections noting the same-origin shift (paths now relative). Final commit pending.

[2026-05-05T16:10:00Z] COMMIT: 7074f49 "T1 R8: milesymedia ↔ portal stitch (one origin, two folders)". 14 files / +435 LOC / -23 LOC. Single-commit round (the `git pull --rebase` reported "Cannot rebase onto multiple branches" mesh hazard but the commit pushed cleanly to origin/main). R8 closed.

[2026-05-05T16:35:00Z] PROGRESS: autonomous wake post-R8 DONE. Inbox shows commander's 15:05Z REPLY validating my Q-ASSUMED on Next.js rewrites over a custom dev-proxy ("exactly the right call. T6's R1 Phase A Vercel monorepo config — commits 359b476 + b3d7944 chapter — is the production-side equivalent"). My R8 already merged with T6's monorepo work via `next.config.ts.beforeFiles` mirroring root `vercel.json`'s rewrites. tasks.md "In progress" still listed T1 R8 as in-flight (orchestrator's update predated my DONE landing) — replaced with `_(T1 R8 done — see Done — Round 8 below)_` pointer to commits 7074f49 + c2dc0f1. No R9 TASK in inbox yet; ending the autonomous loop here. T1 standing down until next prompt.

[2026-05-05T17:00:00Z] STARTED: Round 9 OAuth providers. R9 TASK landed at 15:50Z. Three goals: A) Google OAuth (`/api/auth/oauth/google/{start,callback}`) — agency + client tier login via Google ID token; env-gated provider, hidden when keys absent. B) Magic-link sign-in for end-customers (`/api/auth/magic/{request,verify}` + `/login/magic` consume page) — HMAC-signed single-use 15-min token, delivered through T2 R10's email-sender. C) LoginForm + embed update — Google button + magic-link toggle. Pre-reads queued (auth.ts R1 chapter, end-customer R5 chapter, architecture §3, T2 R10 EmailService.enqueue contract). Q-ASSUMED logged: when email-sender plugin isn't installed for the client's agency, magic-link request returns 503 with a clear "magic-link not configured" message rather than failing silently — handler reads pluginInstalls to decide; falls back to logging the URL only when explicit `NEXT_PUBLIC_DEV_BYPASS=1` is set. Q-ASSUMED #2: existing `lk_session_v1` cookie shape stays unchanged — no schema bump for OAuth/magic; the cookie is just issued through new entry points.

[2026-05-06T00:00:00Z] PROGRESS: /loop re-fired the R8 prompt. R8 shipped on origin/main at `7074f49` (chapter 48 `04-milesymedia-portal-stitch.md`, MASTER row 48, tasks.md "Done — Round 8" entry, R4+R5 chapter appendices). Per loop discipline (won't silently switch loops to a different round), not redoing R8 and not switching to R9 from an R8 prompt. R9 (`T1-round9-oauth-providers.md`) was STARTED at 17:00Z and remains the actual in-flight round; awaiting fresh /loop with the R9 prompt to resume. Ending this fire here.

[2026-05-06T00:30:00Z] DONE: T1 R9 OAuth providers + magic-link complete. Goal A Google OAuth: `lib/server/oauthGoogle.ts` (env-gated, HMAC state, tokeninfo verify) + `/api/auth/oauth/google/{start,callback}` routes with first-run bootstrap path. Goal B magic-link: `lib/server/magicLink.ts` (HMAC 15-min single-use, in-memory nonce store, pluggable delivery hook for T2 R10 email-sender) + `/api/auth/magic/{request,verify}` routes + `/login/magic` consume page. Goal C: LoginForm/EmbedLogin updated with Continue-with-Google button + magic-link toggle + new `"magic"` mode. Smoke 18/18 pass (`scripts/smoke-auth-{oauth,magic}.test.ts` via tsx --test, 11+7 cases covering env gating, state round-trip + bad-sig + malformed reject, tokeninfo happy/audience-mismatch/expired/missing-id_token, magic round-trip + tampered + expired + replay + delivery-hook registered/unregistered). npm aliases `smoke:auth-oauth` + `smoke:auth-magic`. tsc clean. Chapter 55 `04-foundation-round9-oauth-magic.md` + MASTER row 55 + tasks.md row [x]. Q-ASSUMED logged: tokeninfo over JWKS for v1 (no extra dep, fewer moving parts; jose deferred R10); OAuth-bootstrapped accounts get random unguessable password until `authProviders` field lands; smoke files .test.ts not .mjs (matches T6 R2's smoke-vercel-domain.test.ts precedent — TypeScript helpers under test). Cross-team handoffs: T2 R10 register MagicLinkDelivery hook at email-sender boot; T6 R2 set `GOOGLE_OAUTH_REDIRECT_URI` env in prod (one-line deploy runbook §3 addition); T4 polish swap `🔐` placeholder for official Google glyph. R10+ deferred: shared magic-nonce store, JWKS verify, authProviders field, agency-side password reset, additional OAuth providers (Microsoft/Apple/GitHub all explicitly out of scope per prompt). Final commit pending.

[2026-05-07T00:00:00Z] PROGRESS: /loop re-fired the stale R9 prompt. R9 shipped @ `ef8b494` (Google OAuth + magic-link, MASTER #55, smoke 18/18). Inbox carries R10 TASK at `T1-round10-encryption-and-rls.md` (per-install secret encryption + Postgres RLS). Per loop discipline, won't silently switch loops to R10 from an R9 prompt. Ending this fire. Awaiting fresh /loop with the R10 prompt.

[2026-05-07T00:50:00Z] DONE: Agency Shell shipped. /portal/agency rewritten as Ed's home (Welcome + New-client CTA + clients grid w/ brand mark/phase chip/last-activity/hover-footer; empty-state hero). NEW _NewClientButton.tsx inline modal (auto-slug, presets fetched from fulfillment, Live skips presets per ch.19b). /portal/clients/[id] rewritten as tabbed screen via ?tab= (Overview/Website/Portal/Kanban/Finance/Assets/Tools); thin _OverviewTabs.tsx client component, all tab content server-rendered. _ToolsPicker.tsx "+ Add capability" picker calls fulfillment marketplace endpoints (install/enable/uninstall) — plugins in current phase preset show 'from preset' chip. Sidebar.tsx extended with optional extra ReactNode slot; agency layout passes <AgencyToolsBallpark/> collapsible Tools group (HR/Finance/Marketing/Forms/Email/Ops/Domains/Affiliates). Smoke extended with §Agency shell block (home 200+strings, all 7 tabs 200, add-client POST 200/201). HARD BOUNDARY honoured — zero touches to milesymedia/ or business-os/. tsc clean. Chapter 04-agency-shell.md + MASTER row #59 + tasks row.
[2026-05-07T00:51:00Z] COMMIT: pending — committing now.

[2026-05-07T01:05:00Z] COMMIT: 04f7186 — T1 Agency Shell paperwork (MASTER #60 + tasks row + this DONE entry). Note: my code edits arrived at the same files concurrently with upstream c293792 (orchestrator's parallel ship of the same scope); autostash on pull merged identical content and git treated my .tsx edits as no-ops vs HEAD. Net deliverable from this cycle = the MASTER row + tasks row. R+1 follow-up acknowledged from orch NOTE: fold in Aqua HQ specifics (therapist modal fields, real phase progression Epic Intro → Mastery, Aqua HQ 6-section sidebar replacing the placeholder Tools list, "Where Healing Meets Revolution" welcome copy). Awaiting next prompt.

[2026-05-07T01:30:00Z] DONE: Aqua reskin shipped. Goal A — fulfillment DEFAULT_PHASE_PRESETS replaced with Aqua's six (Epic Intro → Mastery & Ascension) + Churned tail; ClientStage union extended additively in foundation types.ts + fulfillment tenancy.ts with aqua-* ids (legacy stages kept so demo seed + Live flag still compile); phaseLabel learned the new labels. Goal B — _NewClientButton rewritten: therapist + practice name (composed display, auto slug from composed value), plan tier select (Foundational / Expansion / Mastery w/ hints), starting Aqua phase (default aqua-epic-intro), WhatsApp + Stripe URLs, £100 lock-in checkbox; submits metadata bag. Goal C — Client.metadata? added foundation-side, threaded through tenants (merge-on-update), plugins/_types, clientStoreAdapter, fulfillment ports/lifecycle/handler. Goal D — AgencyToolsBallpark rewritten as the canonical Aqua HQ six sections + collapsed "More tools" group with HR/Forms/Email/Ops/Domains/Affiliates. Goal E — tagline subtitle "Where Healing Meets Revolution." on home; audience-framed empty + active state copy; per-client header gained plan tier caption + Lock-in paid chip; Overview tab gained WhatsApp + Stripe quick-action buttons (only render when metadata set). Goal F — smoke "§ Aqua reskin" block: preset endpoint returns six aqua-* ids, home body contains tagline + "Aqua HQ", add-client w/ full metadata persists, overview body shows plan tier label + WhatsApp action + Lock-in chip. HARD BOUNDARY honoured (no milesymedia/ or business-os/ touches). tsc clean (portal + fulfillment). Chapter 04-agency-shell.md Round-2 section appended. MASTER row #62. tasks row added.

[2026-05-07T03:00:00Z] DONE: queue file `001-aqua-reskin.md` — work was already shipped (chapter `04-agency-shell.md` Round-2 section, MASTER row #62, tasks.md `T1 Agency Shell R2 — Aqua reskin` ticked, code merged via orchestrator commit `65c2e53` + earlier Aqua reskin landing). Verified all six goals A-F present in repo: fulfillment `DEFAULT_PHASE_PRESETS` carries the six aqua-* phases; `_NewClientButton.tsx` has therapist + practice + plan tier + WhatsApp + Stripe + lock-in fields; `AgencyToolsBallpark.tsx` renders Aqua HQ six sections + collapsed "More tools"; agency home has the "Where Healing Meets Revolution." subtitle + audience-framed empty/active copy; per-client overview surfaces plan tier caption + Lock-in chip + WhatsApp/Stripe quick actions. New queue-based router-driven /loop fired this cycle — first wake under the new architecture. Awaiting archive of 001 → next wake will pick up `002-employee-hq.md`.

[2026-05-07T03:15:00Z] STARTED: queue 002-employee-hq.md (Employee HQ + Role Builder). Pre-reads done (chapter #59 §9 + §9c Employee HQ spec, chapter 04-agency-shell.md R2 baseline, agency-hr existing domain.ts/staff.ts/departments.ts/foundationAdapter/manifest, plugins/_types.ts shape). Plan: A) extend agency-hr `Staff` additively with `agencyEmployee`, `customRoleId`, `assignments[]` (defaults safe for back-compat). B) new `Role` domain + `RoleService` in agency-hr (storage `role:<id>` + `role/index`; seedDefaults idempotent via onInstall hook); `PermissionKey` union covers prompt's 14 keys + 5 sops.tag.<family> variants. C) two new admin pages mounted via the manifest — `EmployeesPage` (list + invite-staff via existing StaffService with `agencyEmployee:true` flag) and `RolesPage` (matrix grid, default roles read-only). EmployeeProfilePage v1 inline expand within EmployeesPage rather than its own route, to keep the manifest pages concise. D) `requirePermission` helper exported from agency-hr/server alongside the existing role gate (foundation route-level `requires:` declaration plumbed but not retro-fitted across all plugins — documented as R+1). E) sidebar role-aware filter: visibleViewIds threading is documented in chapter as the wire-up point; the chrome's `Sidebar.tsx` already drops items via `visibleToRoles` so the gate works through the existing path with the new role label. F) smoke `agency-hr roles` block + chapter `04-employee-hq.md` + MASTER row + tasks row. Q-ASSUMED: EmployeeProfilePage merged into EmployeesList row-expand (NOT a separate page route) — keeps file count tight; mark deferral in chapter. Q-ASSUMED: NDA-signed flag + payroll-link surfaced as Staff `metadata` (matches the same `metadata: Record<string, unknown>` shape the agency-shell R2 added on Client) rather than first-class fields, since the prompt §C only flags them as profile-page surfaces. Q-ASSUMED: `requires: PermissionKey[]` route declaration honored by an opt-in `permissionGuard()` helper plugins call in their handlers; no sweeping foundation refactor of every existing route — incremental adoption documented.

[2026-05-07T03:45:00Z] DONE: queue file `002-employee-hq.md` shipped. Employee HQ + Role Builder live on `agency-hr` plugin per chapter #59 §9 + §9c. Goal A — Staff additively extended (`agencyEmployee?`, `customRoleId?`, `assignments?`, `metadata?` w/ merge-on-update). Goal B — NEW `RoleService` (`src/server/roles.ts`) + `CustomRole` domain + 18-key `PermissionKey` union (14 prompt + 5 `sops.tag.<family>`); `seedDefaults` idempotent (Founder/Admin/Designer/Copywriter/Ops); seed rows refuse mutation; onInstall extended. Goal C — NEW `EmployeesPage` + `EmployeeListClient` (row-expand profile inline; merged the dedicated `/employees/[id]` route into row-expand per Q-ASSUMED) + NEW `RolesPage` + `RoleMatrixClient` (sticky role col + 18 perm cols + checkbox grid + Clone-seed action). Goal D — `permissionGuard(role, requires: PermissionKey[])` exported as opt-in 403-throw; `roleHasPermission` predicate. Goal E — data-side `visibleViewIds` wired; chrome `effectiveRole` resolver R+1 deferred (documented). Goal F — smoke `§ Employee HQ` block (roles GET 200 + 5 seed roles seed:true + clone POST + invite employee POST). 4 new API routes (`GET/POST/PATCH/DELETE roles`); 2 new nav items + 2 pages registered in manifest. tsc clean (agency-hr workspace). HARD BOUNDARY honoured. Chapter `04-employee-hq.md`; MASTER row #65; tasks.md row added under "In progress" Done block. WARN — parallel-session mesh hazard hit mid-flight: orchestrator commit `0650856` absorbed `RoleMatrixClient.tsx` from untracked tree + a working-tree reset lost `roles.ts` + `EmployeesPage.tsx` + `RolesPage.tsx` + `EmployeeListClient.tsx` after their Write tool calls reported success. Re-applied + locked in via per-step commits (`15c48f1` + `369b8fd`). Same `.git/index` race documented in R7/R8 chapters. Awaiting commander archive of 002 → next wake picks up `003-live-phase-builder.md`.

[2026-05-07T04:00:00Z] WAKE-PENDING-ARCHIVE: 002-employee-hq.md still lowest in queue (DONE logged at 03:45Z, commit `f2cbd45`). Awaiting commander archive to pick up `003-live-phase-builder.md` next wake. Sleeping 1800s.

[2026-05-07T01:47:00Z] STARTED: queue 003-live-phase-builder.md (Live phase — custom portal builder gateway). Pre-reads done (architecture extension ch.19b, portal-export plugin: routes mounted at `/api/portal/portal-export/...`, primary endpoint = `clients/export` POST taking `{clientId, options:{presetId,dryRun}}`; presets = service-portal/skincare-brand/membership-only/affiliate-only). Plan: A) page.tsx — Live detection helper (stage===`aqua-mastery`||`live`); fs check `clients/<slug>/` exists → CTA flips Build/Open; "Live" badge appended to phase chip. B) NEW _BuildPortalWizard.tsx client modal — checkbox plugin list + preset/template radio + slug confirm; calls `POST /api/portal/portal-export/clients/export` (Q-ASSUMED: prompt's `/materialize` endpoint maps to `clients/export` since that's what plugin actually exposes — confirmed in ExportPage.tsx form action), polls `/history` for completion. C) ToolsPicker — when `isLive` show "Recommended for Live" callout w/ ecommerce+memberships+affiliates+agency-marketing list + one-click installAll button. D) smoke `§ Live phase gateway` + chapter `04-agency-shell-live-phase.md` + MASTER row + tasks row. Q-ASSUMED: PhaseDefinition has no `tier` field in foundation types — keying off `aqua-mastery`/`live` stage enum directly per prompt's "or any phase tagged tier:live" fallback (no schema change for v1). Q-ASSUMED: synchronous export call + redirect on response (no streaming progress in v1) since `runExportHandler` already returns the full record body in one shot — matches portal-export ExportPage.tsx form-post pattern.

[2026-05-07T01:55:00Z] DONE: queue file `003-live-phase-builder.md` shipped. Goal A — page.tsx Live detection (`aqua-mastery` ∪ legacy `live`) + `customPortalExists()` via `node:fs.existsSync` on `<cwd>/../clients/<slug>/`; header gains amber Live badge next to the phase chip + a CTA slot that flips between **Build custom portal** wizard (folder missing) and **Open custom portal ↗** anchor (folder present). Goal B — NEW `_BuildPortalWizard.tsx` client modal: plugin checklist (pre-checked = installed, recommended chips highlight the architecture-extension §5a Live set), base-template radio (blank starter / luv-and-ker / compass + any portal-export presets pulled lazily on open from `GET /api/portal/portal-export/presets`), slug confirm; submit POSTs `/api/portal/portal-export/clients/export` with `{clientId, options:{presetId, destinationOverride, installedPluginsHint}}`; v1 synchronous run + `router.refresh()` on success. Q-ASSUMED: prompt's `/materialize` endpoint maps to `clients/export` since that's the actual run route per `plugins/portal-export/src/api/routes.ts` (the plugin's own ExportPage form-action confirms). Q-ASSUMED: no streaming progress yet — `runExportHandler` returns the full ExportRecord in one shot, polish can land on a future round when a job-queue surface exists. Goal C — ToolsPicker grew `isLive` + `liveRecommended` optional props; when Live, renders an amber callout naming the recommended set (website-editor · client-crm · forms · ecommerce · memberships · affiliates · agency-marketing) + the missing subset, with a one-click "Install Live recommended" button that loops `marketplace/install` per missing pluginId then refreshes. Goal D — smoke `§ Live phase gateway` block: aqua-mastery client overview shows the Live badge + Build CTA + Recommended-for-Live callout; aqua-blueprint client omits the CTA. Q-ASSUMED: foundation `PhaseDefinition` carries no `tier` field, so the prompt's "tier: live" tag falls back to the stage enum directly — no schema change for v1. Chapter `04-agency-shell-live-phase.md` (NEW); MASTER row #75 (next free integer after 74); tasks.md row added under "In progress" Done-style block. tsc clean. HARD BOUNDARY honoured (zero touches to milesymedia/ or business-os/). WARN — mesh-absorption hit again: my edits to page.tsx + ToolsPicker.tsx + smoke.mjs + MASTER.md + tasks.md + the new _BuildPortalWizard.tsx + new chapter file all landed inside parallel commander commit `9819720` ("Cycle 38 — workers re-engaged") rather than a dedicated T1 commit; verified post-pull that working-tree diffs against HEAD are empty AND all expected content is present (ToolsPicker has the new `isLive` prop, _BuildPortalWizard.tsx exists in HEAD, page.tsx has the Live block, MASTER row #75 present, tasks row present). All work shipped to origin/main; net deliverable from this cycle = the outbox DONE entry. Awaiting commander archive of `003-live-phase-builder.md` → next wake picks up `004-sops-and-resources-surfacing.md`.

[2026-05-07T01:56:00Z] WAKE-PENDING-ARCHIVE: 003-live-phase-builder.md still lowest in queue (DONE logged at 01:55Z, mesh-absorbed into commit 9819720 + outbox-only commit 878268d). Awaiting commander archive to pick up `004-sops-and-resources-surfacing.md` next wake. Sleeping 270s.

[2026-05-07T02:00:00Z] WAKE-PENDING-ARCHIVE: 003-live-phase-builder.md still lowest in queue (DONE at 01:55Z, mesh-absorbed into 9819720; outbox commits 878268d + 526a128). Sleeping 1800s.

[2026-05-07T02:30:00Z] WAKE-PENDING-ARCHIVE: 003-live-phase-builder.md still lowest (DONE at 01:55Z). Sleeping 1800s.

[2026-05-07T03:00:00Z] STARTED: queue 004-sops-and-resources-surfacing.md (Surface SOPs in agency sidebar + per-client SOPs tab + permission gates). Pre-reads: T2 sops plugin (mount path /portal/agency/sops, API /api/portal/sops/{list,tags,...}, TagFamily = sales|service|leads|standards|mastery, Sop has updatedAt:number); T1/002 roles.ts permissionGuard helper (currently per-plugin context); chapter #59 §9c. Plan: A) AgencyToolsBallpark gains an inline async fetch of `/api/portal/sops/list` on mount, computes count of sops with updatedAt within last 7d, renders compact "· N updated this week" tail next to the SOPs row label. B) Per-client overview adds a `sops` sub-tab; phase→family map (aqua-epic-intro/aqua-blueprint→sales+leads, aqua-diagnostics/aqua-brand-builder→service, aqua-traffic/aqua-mastery→service+mastery, legacy stages→standards); list filtered by mapped family, read-only links to /portal/agency/sops/read/<slug>. C) NEW `lib/server/sopsAccess.ts` exporting `assertSopsAccess(session, family?)` — Founder fallback (agency-owner/agency-manager full pass; agency-staff allowed for read in v1 + deferred deeper grid hookup; client/account roles 403); applied on the per-client SOPs tab render. Q-ASSUMED: foundation can't easily invoke agency-hr's RoleService (scoped plugin storage) so v1 keys off session.role enum directly with Founder default per prompt's last bullet — deeper CustomRole permission lookup deferred to R+1 once a foundation-level "current employee role" resolver lands. Q-ASSUMED: phase→family map kept inline in page.tsx (one file owns the agency-shell experience) rather than a new server module — six entries, not worth its own service. D) smoke `§ SOPs surfacing` block (sidebar fetch fires, per-client SOPs tab renders 200 + shows family heading, account-role denial returns 403); chapter `04-agency-shell-sops.md`; MASTER row; tasks row.

[2026-05-07T03:15:00Z] DONE: queue 004-sops-and-resources-surfacing.md shipped @ commit c318700. Goal A: `AgencyToolsBallpark` gained inline mount fetch of `/api/portal/sops/list` + emerald "{N} new" chip on the SOPs row when ≥1 SOP `updatedAt` within 7d (silent on plugin-not-installed). Goal B: per-client overview gains a `sops` sub-tab between `assets` and `tools`; NEW `_ClientSopsTab.tsx` fans out per-family fetches (`?tag=<family>&status=published`), de-dupes by id, sorts by updatedAt desc, read-only links to agency shelf's `/read/<slug>`. Goal C: NEW `lib/server/sopsAccess.ts` exporting `assertSopsAccess(session, family?)` (Founder fallback — any agency-* role passes v1; client/end-customer 403 with `SopsAccessError.status=403`) + `familiesForStage(stage)` phase→family map (lead/discovery/aqua-epic-intro/aqua-blueprint→sales+leads · design/development/onboarding/aqua-diagnostics/aqua-brand-builder→service+standards · aqua-traffic→service+mastery · live/aqua-mastery→mastery+service · default→standards). 403 surfaces inline on tab. Q-ASSUMED: foundation can't reach agency-hr's RoleService (per-plugin storage scope) so v1 keys off `session.role` per prompt's "Founder default" bullet; deeper CustomRole permission grid lookup deferred R+1 (foundation needs an employee-role resolver first). Q-ASSUMED: phase→family map kept inline in sopsAccess.ts (six entries, not worth its own service). Q-ASSUMED: per-client tab is client-component fetch (not server-rendered) — plugin storage isn't reachable from foundation server tree without registered service; client `useEffect` keeps the boundary clean and reuses the plugin's existing HTTP `visibleToRoles` enforcement. Goal D: smoke `§ SOPs surfacing` block (list 200 + per-client `?tab=sops` 200 + "SOPs for" heading + agency-shelf link visible). Chapter `04-agency-shell-sops.md`. MASTER row #77 (next free integer after 76). tasks.md row added. tsc clean. HARD BOUNDARY honoured (sops plugin untouched). Awaiting commander archive of 004 → next wake picks up `005-founder-todos-home-widget.md`.

[2026-05-07T03:16:00Z] WARN: mesh-absorption — my DONE-commit `a15b653` accidentally bundled T4's incubator-app work (12 files: incubator.css/js/html, onboarding/portal-bridge/resources/discover.html, business-os/bos.js patch, plus T4 chapter `04-incubator-phase-portal.md` + tasks/outbox edits) via autostash. T4's work landed cleanly on origin/main inside my commit; no data loss but commit attribution is wrong. Same `.git/index` shared-tree hazard documented in chapters #71 + recurring across rounds. T1 didn't touch milesymedia/ or business-os/ in any of its actual edits — verify by inspecting `c318700` (the real T1 R4 commit) which only contains SOPs surfacing files. Continuing to next round per loop discipline.

[2026-05-07T03:17:00Z] WAKE-PENDING-ARCHIVE: 004-sops-and-resources-surfacing.md still lowest (DONE at 03:15Z, commit c318700; queue depth has grown to 10 with rounds 005-013 staged). Sleeping 1500s.

[2026-05-07T03:38:00Z] STARTED: queue 005-founder-todos-home-widget.md. 004 archived. Plan: NEW `_FounderTodosWidget.tsx` (client component) mounted on `/portal/agency` home above the clients grid; props pass `isFounder=session.role==="agency-owner"`. Widget: lists boards via `/api/portal/kanban/boards?role=founder`, finds `templateId==="founder-todos"` (auto-creates one via POST `/boards` if absent so first-load isn't empty — Q-ASSUMED), fetches cards via `/boards/cards?boardId=...&status=active`, filters to Today + This Week columns, shows 5 most recent by updatedAt. Inline `+ Add task` POSTs to `/boards/cards` with Today columnId. Click card → `/portal/agency/kanban/boards/<id>` (BoardDetailPage). Renders nothing for non-founder (no zero-space wrapper). Mythos copy "Today's Quests" / "No quests today. Forge one." Smoke `§ Founder todos` block (widget visible via founder POV; `+ Add` POST 201; non-founder POV omits widget).

[2026-05-07T03:48:00Z] DONE: queue 005-founder-todos-home-widget.md shipped @ commit 4320675. Goal A: NEW `_FounderTodosWidget.tsx` client component on `/portal/agency` home (above clients grid). Boots via `GET /api/portal/kanban/boards?role=founder`, finds `templateId==="founder-todos"`, auto-creates the board (POST `/boards` with templateId+scope=agency) when absent — Q-ASSUMED, kanban server still enforces `requiresRole:"founder"` + `requiresScope:"agency"` guards. Then `GET /boards/cards?boardId=<id>&status=active`, filters Today + This Week columns by columnId, sorts updatedAt-desc, slice(0,5). Inline `+ Add quest to Today` form POSTs `/boards/cards` with Today columnId + tags:["founder"]; optimistic append. Click → `/portal/agency/kanban/boards/<id>#card-<cardId>`. Returns `null` for non-Founder (zero-space, not hidden div). Goal B: mythos copy lifted from §1 + §11 — header `Today's Quests`, subtitle `Founder-only · Today + This Week`, empty `No quests today. Forge one.`, input ph. `+ Add quest to Today`, CTA `Open board →`. Q-ASSUMED: founder=agency-owner since foundation has no separate founder enum (kanban accepts literal "founder" via `?role=` matcher case-insensitively). Goal C: smoke `§ Founder todos widget` block (home 200 founder POV + "Today's Quests" + `founder-todos-widget` testid + `kanban/boards?role=founder` 200). Chapter `04-agency-shell-founder-todos.md`. MASTER row #80. tasks.md row added. tsc clean. HARD BOUNDARY honoured (kanban plugin internals untouched). Awaiting commander archive of 005 → next wake picks up `006-onboarding-dashboard.md`.

[2026-05-07T03:49:00Z] WAKE-PENDING-ARCHIVE: 005-founder-todos-home-widget.md still lowest (DONE at 03:48Z, commit 4320675). Sleeping 1500s.

[2026-05-07T04:01:00Z] STARTED: queue 006-onboarding-dashboard.md (Onboarding Dashboard for active clients). Plan: A) NEW `lib/server/onboardingMilestones.ts` with AQUA_MILESTONES seed (per chapter §5+§5a — Epic Intro: welcome+discovery+gift; Blueprint: brand-audit+system-form+playbook; Diagnostics: foundations-report+website-draft; Brand: logo-colours+verification+photo; Traffic: first-ad+first-lead+first-sale; Mastery: 100-reviews+200-reviews+retainer-signed); helpers `getMilestoneState(client,phaseStage)` + `allComplete(client,phaseStage)`. B) NEW `app/api/tenants/onboarding-tick/route.ts` — POST `{clientId, phaseStage, milestoneId, done}`, gated via `requireRoleForClient(AGENCY_ROLES, clientId)`, calls `updateClient` merging into `metadata.onboardingProgress[phaseStage]`. C) NEW `_OnboardingDashboardPanel.tsx` client component: 6-chip horizontal strip (visited phases ticked from `phases.order < currentOrder`, current highlighted, future muted), click chip → expands deliverables list with checkboxes (POST tick endpoint), advance button on current chip (validates allComplete → POSTs `/api/portal/fulfillment/phase/advance` with `fromPhaseId/toPhaseId` resolved from `phases.find(p=>p.stage===…)`). D) wired into per-client overview Overview tab above recent-activity, only when `client.stage` starts with `aqua-`. Smoke `§ Onboarding dashboard`. Q-ASSUMED: phaseStage used as key (ClientStage enum) since chapter speaks of "PhaseId" but fulfillment's phases.id is per-agency UUID — keying off the stable stage enum keeps milestone state intact across re-seeded phase rows. Q-ASSUMED: visited-phase tick = phase.order strictly less than current.order; current = stage match; future = strictly greater. Q-ASSUMED: tick endpoint uses foundation `updateClient` direct (no fulfillment route) since it's pure metadata bookkeeping with no side-effects, and avoids a fulfillment-side schema change.

[2026-05-07T04:14:00Z] DONE: queue 006-onboarding-dashboard.md shipped @ commit f20a875. Goal A: NEW `_OnboardingDashboardPanel.tsx` (client component) on per-client Overview tab above the 2-col grid — six-chip horizontal phase strip (palette: emerald=complete · brand-primary highlighted=active · muted=future), each chip carries `{done}/{total}` glyph; click toggles expanded deliverables pane (default = current stage). Goal B: NEW `lib/server/onboardingMilestones.ts` with `AQUA_PHASE_ORDER` + `AQUA_MILESTONES` seed for all six Aqua phases per prompt's deliverable list (Epic Intro: welcome/discovery/gift; Blueprint: brand-audit/system-form/playbook; Diagnostics: foundations/website-draft; Brand: logo-colours/verification/photo; Traffic: first-ad/first-lead/first-sale; Mastery: 100-reviews/200-reviews/retainer); helpers `getMilestoneState`, `isPhaseComplete`, `tickMilestone`. Storage shape `client.metadata.onboardingProgress: Partial<Record<ClientStage, [{id,done,doneAt?}]>>` (Q-ASSUMED: keyed by stable ClientStage enum not per-agency PhaseDefinition.id UUID — survives re-seed). NEW `POST /api/tenants/onboarding-tick` foundation route, gated `requireRoleForClient(AGENCY_ROLES)`, validates Aqua-stage + known milestoneId, persists via `updateClient(metadata:{onboardingProgress})` (400/404/500 error shapes). Q-ASSUMED: foundation `updateClient` direct, not a fulfillment route — pure metadata bookkeeping with no domain side-effects. Goal C: active chip carries `Mark phase complete → advance` button gated on `allComplete`; POSTs existing `/api/portal/fulfillment/phase/advance` with `fromPhaseId/toPhaseId` resolved by sorting agency PhaseDefinitions by AQUA_PHASE_ORDER index (Q-ASSUMED: ordering by canonical stage list, not per-agency `phases.order` field, keeps strip stable across agency reorders). Panel auto-hides for non-Aqua stages. Goal D: smoke `§ Onboarding dashboard` block (aqua-blueprint client overview shows `onboarding-dashboard` testid + "Onboarding journey" heading + tick endpoint 200 + unknown-milestone 400 + legacy-stage client omits panel). Chapter `04-agency-shell-onboarding-dashboard.md`. MASTER row #81. tasks.md row added. tsc clean. HARD BOUNDARY honoured. Awaiting commander archive of 006 → next wake picks up `007-effective-role-resolver.md`.

[2026-05-07T04:15:00Z] WAKE-PENDING-ARCHIVE: 006-onboarding-dashboard.md still lowest (DONE at 04:14Z, commit f20a875). Sleeping 1500s.

[2026-05-07T04:30:00Z] STARTED: queue 007-effective-role-resolver.md (Effective-role resolver in chrome). Plan: A) NEW `lib/server/effectiveRole.ts` exporting `effectiveRole(session): { roleLabel, permissions: PermissionKey[], isFounder }`. v1 keys off `session.role` enum directly mapping to agency-hr's DEFAULT_ROLES seed labels (agency-owner→Founder full perms; agency-manager→Admin all-minus-roles.edit; agency-staff→Designer narrow; client-*/end-customer→[]). Re-exports `PermissionKey` from `@aqua/plugin-agency-hr` (workspace dep so import path is clean). The deeper Q-ASSUMED — read user's `agencyEmployee.customRoleId` from agency-hr storage at request time — is documented as the foreseen hookup point but not wired in v1 since foundation has no container resolver for plugin storage. B) Extend `NavItem` in `plugins/_types.ts` with optional `requires?: PermissionKey[]`; `BuildSidebarInput` gains `permissions?: readonly PermissionKey[]`; `buildSidebar` adds an `if (item.requires && !item.requires.every(p => perms.has(p))) continue;` gate (Founder bypass: empty-permissions input means "all"). C) NEW `<RequirePermission requires={[…]}>` server-component wrapper in `lib/server/RequirePermission.tsx` that throws/renders 403 when caller's effectiveRole lacks any of the required keys; migrate fulfillment phase-transition + agency-hr roles page + agency-finance placeholder to use it. D) smoke `§ Effective role` (Founder POV → all sidebar items visible; resolver returns 18 perms for agency-owner; 17 for agency-manager; 3 for agency-staff). Q-ASSUMED: actual customRoleId override deferred R+1 (needs foundation→plugin storage resolver — not built in this round); Q-ASSUMED: empty-permissions input from BuildSidebarInput treats nav as "no gate" (Founder default) since most legacy plugins don't declare `requires` yet — once they do, the empty-perms case will be replaced with explicit Founder-isn't-empty signaling.

[2026-05-07T04:48:00Z] DONE: queue 007-effective-role-resolver.md shipped @ commit 20c94cc. Goal A: NEW `lib/server/effectiveRole.ts` — `effectiveRole(session): {roleLabel, permissions, isFounder}` mapping session.role enum to agency-hr DEFAULT_ROLES seed by label (agency-owner→Founder/18 perms · agency-manager→Admin/17 · agency-staff→Designer/3 · client-*/end-customer→[]); `hasAllPermissions(eff, requires)` with Founder bypass + empty-requires=no-gate. Re-exports `PermissionKey` + `ALL_PERMISSION_KEYS`. Q-ASSUMED: customRoleId override deferred R+1 (foundation has no plugin-storage container resolver yet); relative-path import of `agency-hr/src/server/roles.ts` since the package's node_modules snapshot pre-dates the new exports — clean reinstall flips this back to `@aqua/plugin-agency-hr/server`. agency-hr/src/server/index.ts now re-exports `ALL_PERMISSION_KEYS` + `PermissionKey` + `CustomRole` for the next reinstall. Goal B: extended `NavItem.requires?: string[]` in `_types.ts`; `BuildSidebarInput` gains `permissions?` + `isFounder?`; `buildSidebar` adds Founder-bypass `requires`-intersection filter immediately after the existing `visibleToRoles` gate; both agency + per-client layouts compute `effectiveRole(session)` and pass through. Goal C: NEW `<RequirePermission session requires fallback?>` server component renders inline 403 panel naming required keys + user's roleLabel. Migrations: per-client Tools tab gated on `plugins.install`; Finance tab on `finance.view`. Deferred (chapter documents one-line migration path once foundation page-mount adapter lands): agency-finance admin pages, agency-hr roles page, fulfillment phase/advance API. Goal D: smoke `§ Effective role` (3 checks — Founder POV → tools + finance tab content visible w/o 403 panel substring); deeper role-swap smoke across all 5 seed roles deferred (would need distinct demo sessions per role — out of scope for this round). Chapter `04-effective-role-resolver.md`. MASTER row #82. tasks.md row added. tsc clean. HARD BOUNDARY honoured. Awaiting commander archive of 007 → next wake picks up `008-client-tasks-kanban-tab.md`.

[2026-05-07T04:49:00Z] WAKE-PENDING-ARCHIVE: 007-effective-role-resolver.md still lowest (DONE at 04:48Z, commit 20c94cc). Sleeping 1500s.

[2026-05-07T05:00:00Z] STARTED: queue 008-client-tasks-kanban-tab.md (per-client Kanban tab wired to T2's client-tasks template). Plan: A) NEW `_KanbanTabClient.tsx` client component — fetches `/api/portal/kanban/boards`, finds `clientId===<this>` + `templateId==="client-tasks"`, auto-creates board (POST `/boards` with `{name:"<client> tasks", scope:"client", clientId, templateId:"client-tasks"}`) if absent, then fetches cards and renders 6-column horizontal scroll grid (Backlog · This Week · Doing · Waiting On Client · Review · Done). B) Backlog column hosts pinned-bottom quick-add input (`<form>`-submit POSTs `/boards/cards` with Backlog columnId + tags=["client"]); drag handlers (HTML5 native, lightweight) POST `/boards/cards/move` on drop. C) "Waiting On Client" column gets amber palette (border-amber-300 bg-amber-50/60); count of cards in that column surfaces as a chip in the per-client header (server-side fetch from page.tsx). D) Wire into per-client overview: replace the existing kanban-tab placeholder ("T2's kanban plugin will surface here when shipped") with `<KanbanTabClient clientId={...} />`; smoke `§ Client tasks kanban` (kanban tab 200, board auto-creation works, quick-add POST 201, header shows waiting-count chip when cards exist). Q-ASSUMED: drag is HTML5 native — no `react-dnd` dep; works for desktop-mouse interactions, touch-drag falls back to up-arrow/down-arrow per-card move buttons. Q-ASSUMED: server-side waiting-count fetch in page.tsx via direct kanban API call (not a separate cached server function) since the kanban plugin storage isn't reachable from foundation directly — page.tsx will pass an empty placeholder + Tab component populates the count via callback; alternative: skip header chip server-render and let it appear after client mounts (chosen — cleaner, no SSR drift).

[2026-05-07T05:14:00Z] DONE: queue 008-client-tasks-kanban-tab.md shipped @ commit ffad8b6. Goal A: NEW `_KanbanTabClient.tsx` (client component) replaces the dashed-placeholder kanban tab card on per-client overview. Boots: `GET /api/portal/kanban/boards?clientId=`, finds `clientId===<this> && templateId==="client-tasks"`; auto-creates board (POST `/boards?clientId=` with `{name:"<client> — tasks", scope:"client", templateId:"client-tasks"}`) when absent — kanban plugin enforces `requiresScope:"client"` server-side. Then `GET /boards/cards?clientId=&boardId=&status=active`, groups by columnId, sorts by order. Renders 6-column horizontal-scroll grid (Backlog · This Week · Doing · Waiting On Client · Review · Done). Goal B: Backlog hosts pinned quick-add form (`+ New task` input + Add button) POSTing `/boards/cards` with Backlog columnId + tags=["client"]; cards `draggable`, columns are drop targets POSTing `/boards/cards/move` with `{cardId, toColumnId, toIndex}`. Q-ASSUMED: native HTML5 drag (no react-dnd); touch fallback R+1. Goal C: "Waiting On Client" column gets amber palette (`border-amber-300 bg-amber-50/60` + amber heading); count surfaces as `{N} waiting on client` amber chip in the kanban tab header when >0; auto-email on column transition explicitly deferred to T2 R009 per prompt. Q-ASSUMED: chip lives in kanban tab header (not per-client page header) since foundation server can't easily reach plugin storage at SSR time. Q-ASSUMED: auto-create fires on first tab mount (not tied to fulfillment advancePhase event yet — needs foundation event-bus hook surface; same effect for operators since the board exists by the time they look at it). Goal D: smoke `§ Client tasks kanban` (3 — kanban tab 200 + `client-tasks-kanban` testid + `boards?clientId=` 200). Chapter `04-client-tasks-kanban.md`. MASTER row #83. tasks.md row added. tsc clean. HARD BOUNDARY honoured. Awaiting commander archive of 008 → next wake picks up `009-comms-widget-whatsapp-email.md`.

[2026-05-07T05:15:00Z] WAKE-PENDING-ARCHIVE: 008-client-tasks-kanban-tab.md still lowest (DONE at 05:14Z, commit ffad8b6). Sleeping 1500s.

[2026-05-07T05:30:00Z] STARTED: queue 009-comms-widget-whatsapp-email.md (per-client comms widget — WhatsApp + email + last-contact). Plan: A) NEW `_CommsRow.tsx` client component pinned in per-client header below the name (above phase chip row). Surfaces WhatsApp pill (green w/ logo), Mailto pill (per-client email), last-contact relative-time, "Mark as last contacted" button, "Edit" affordance. B) NEW `app/api/tenants/client-comms/route.ts` POST endpoint accepting `{clientId, patch:{whatsappLink?, clientEmail?, lastContactedAt?:"now"|number}}`, gated `requireRoleForClient(AGENCY_ROLES)`, persists via `updateClient(metadata:{...})`. Inline edit modal lives in CommsRow. "Mark as contacted" sends `lastContactedAt:"now"`. C) Agency home grid tile gains a `💬 {relTime}` chip (or `💬 never` muted when null); amber palette when delta >7d. D) Smoke `§ Comms widget` (POST 200 saves whatsapp + email + lastContactedAt; per-client header surfaces pills; agency home tile shows chip). Q-ASSUMED: existing meta.whatsappLink (stored from R2 reskin) is reused as-is; new key `clientEmail` (was previously `client.ownerEmail` in core schema; here we're storing the dedicated comms-thread email distinct from the portal-account email). Q-ASSUMED: lastContactedAt is metadata bookkeeping only (no fulfillment side-effect) — same shape as onboardingProgress so the same updateClient pathway works.

[2026-05-07T05:32:00Z] DONE: queue 009-comms-widget-whatsapp-email.md shipped @ commit 63f7f30. Goal A: NEW `_CommsRow.tsx` (client component) pinned beneath client-name `<h1>` on per-client header; surfaces emerald WhatsApp pill (anchor → `metadata.whatsappLink`) or dashed `+ WhatsApp` placeholder; blue mailto pill (`mailto:metadata.clientEmail`) or dashed `+ Email`; 🕘 last-contact relative time with amber palette when delta >7d. Goal B: NEW `POST /api/tenants/client-comms` `{clientId, patch:{whatsappLink?, clientEmail?, lastContactedAt?:"now"|number|null}}` — gated `requireRoleForClient(AGENCY_ROLES)`, persists via `updateClient(metadata:{...})`. Light URL/email sanitisation. Q-ASSUMED: empty-string sentinel for cleared values since updateClient shallow-merges (can't true-delete keys); `lastContactedAt=0` (or undefined) treated as absent by all readers. Inline Edit toggle on the row reveals form (WhatsApp + email inputs + Save); Mark-contacted button POSTs `lastContactedAt:"now"`. Goal C: each client tile in `/portal/agency` grid now carries a 💬 chip next to "Last activity" line — `💬 never` muted when unset; `💬 last contact Xd ago` emerald when fresh (≤7d) or amber when stale (>7d) — matches Communication SOP loop-closure spirit. Q-ASSUMED: chip uses `metadata.lastContactedAt` directly from already-loaded client row (no extra round-trip). Goal D: smoke `§ Comms widget` (5 — `client-comms-row` testid + POST 200 + empty-body 400 + saved WhatsApp surfaces in re-rendered header + agency home regex-match `💬 (?:never|last contact)`). Q-ASSUMED: real WhatsApp API + send-email-from-portal explicitly out of scope per prompt. New metadata keys: `clientEmail` (distinct from `client.ownerEmail` portal-account email), `lastContactedAt` (ms epoch, 0=never). Chapter `04-comms-widget.md`. MASTER row #84. tasks.md row added. tsc clean. HARD BOUNDARY honoured. Awaiting commander archive of 009 → next wake picks up `010-files-tab.md`.

[2026-05-07T05:33:00Z] WAKE-PENDING-ARCHIVE: 009-comms-widget-whatsapp-email.md still lowest (DONE at 05:32Z, commit 63f7f30). Sleeping 1500s.

[2026-05-07T05:50:00Z] STARTED: queue 010-files-tab.md (per-client Files tab). T2 R010 `@aqua/plugin-client-files` not yet shipped (verified — no client-files plugin folder); falling back to operator-pasted-link variant per prompt: store references on `client.metadata.files[]` array. Plan: A) NEW `files` tab between SOPs and Tools in `_OverviewTabs.tsx`. NEW `_FilesTabClient.tsx` client component renders 2-col file grid (icon/name/category chip/uploaded-by/uploaded-at + open + delete actions). B) NEW `POST /api/tenants/client-files` route accepting `{clientId, action:"add"|"delete", file?:{name,url,category,uploadedBy?}, fileId?}`, gated `requireRoleForClient(AGENCY_ROLES)`, mutates `metadata.files[]` via `updateClient`. Tab header carries paste-link form (drag-drop UI placeholder + URL input + name + category select). C) Left rail with five categories from prompt (Brand Assets · Brief / Strategy · Deliverables · Invoices · Misc); selecting filters the grid. "All" pseudo-category at top. D) smoke `§ Files tab` (per-client `?tab=files` 200, POST add 200, delete 200, file appears in re-rendered tab). Q-ASSUMED: plugin-not-installed = pure metadata fallback per prompt's explicit fallback bullet (no fileId reservation pattern needed — generated via `crypto.randomUUID()`); drag-drop is visual placeholder only since no S3/upload endpoint (paste-link is the documented v0); shape `{id,name,url,category,uploadedBy?,uploadedAt:number}` minimal but extensible when plugin lands. Q-ASSUMED: existing "Assets" tab (which links to website-editor's image library) is unrelated and stays; new "Files" is a distinct tab for deliverables/contracts/invoices.

[2026-05-07T05:52:00Z] DONE: queue 010-files-tab.md shipped @ commit bf3ee30. T2 R010 `@aqua/plugin-client-files` not yet shipped (verified plugins/ dir) → falling back to operator-pasted-link variant per prompt's explicit fallback bullet; references stored on `client.metadata.files[]`. Goal A: NEW `_FilesTabClient.tsx` two-pane client component (left rail 12rem + right pane 1fr). Right pane: paste-link form (name+URL+category select+Add) on top, then 2-col file grid (emoji+name link · category chip · uploadedBy · relative time · Open + Delete actions). Goal B: NEW `POST /api/tenants/client-files` `{clientId, action:"add"|"delete", file?, fileId?}` — gated `requireRoleForClient(AGENCY_ROLES)`, persists via `updateClient(metadata:{files})`. add validates name+url+category in `{brand,brief,deliverable,invoice,misc}`, assigns id (`crypto.randomUUID` w/ timestamp+rand fallback), uploadedBy defaults to session.email, unshifts onto array (newest-first). delete filters by fileId (404 when missing). 400 on empty body / unknown action / invalid category. Q-ASSUMED: paste-link only — drag-drop UI explicitly omitted as no-op affordance per "real uploads land with T2 R010". Q-ASSUMED: updateClient array replacement (not merge) for `files` since foundation's metadata merge is shallow — passing the whole array overwrites cleanly. Goal C: left rail All + 5 categories per chapter §15c (Brand Assets · Brief / Strategy · Deliverables · Invoices · Misc) with count chips + click-to-filter state; empty-state copy is filter-aware. Q-ASSUMED: 5 fixed categories — no "+ New category" since prompt's left-rail spec is closed. NEW `files` tab inserted between `sops` and `tools` in `_OverviewTabs.tsx`. Goal D: smoke `§ Files tab` (6 — `?tab=files` 200 + `client-files-tab` testid + add 200 returning file.id + empty-body 400 + added file name visible in re-rendered tab + delete 200). Q-ASSUMED: existing `Assets` tab (which routes into website-editor's image library) kept distinct from new `Files` tab (deliverables/contracts/invoices). Chapter `04-files-tab.md`. MASTER row #85. tasks.md row added. tsc clean. HARD BOUNDARY honoured. Awaiting commander archive of 010 → next wake picks up `011-finance-tab.md`.
