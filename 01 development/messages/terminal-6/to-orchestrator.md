# T6 — outbox

Append-only. Format: `[ISO timestamp] TYPE: message` (see `messages/README.md`).

[2026-05-05T00:59:48Z] STARTED: T6 R1 — production deploy + custom domains + observability. Read pre-reads (CLAUDE / messages README / MASTER index / 04-architecture / 04-architecture-extension-per-client-portals / eds requirments / 04-milesymedia-demo / aqua-server-modules / current vercel.json / .vercelignore / .gitignore / 04 the final portal/portal/{package.json,next.config.ts,.env.example} / portal/src/lib/server/auth.ts / portal/src/server/storage.ts / 02's src/lib/vercel/server.ts + api/portal/domains/* / T1-round8 prompt). 9 plugins shipped at file:.. workspace deps in portal/package.json. Existing env: PORTAL_SESSION_SECRET, PORTAL_BACKEND, NEXT_PUBLIC_PORTAL_BASE_URL, DATABASE_URL, NEXT_PUBLIC_DEV_BYPASS, NEXT_PUBLIC_PORTAL_SECURITY.
[2026-05-05T00:59:48Z] Q-ASSUMED: prompt names env vars `LK_SESSION_SECRET` + `STORAGE_BACKEND` + `NEXT_PUBLIC_PORTAL_BASE` — actual code uses `PORTAL_SESSION_SECRET` / `PORTAL_BACKEND` / `NEXT_PUBLIC_PORTAL_BASE_URL` (T1 R1/R7 wired). Honoring T1's existing names; documenting both in the env taxonomy chapter so the rename is visible. Continuing.
[2026-05-05T00:59:48Z] Q-ASSUMED: Phase A monorepo strategy — single Vercel project deploys the Next.js portal (rootDirectory = `04 the final portal/portal`) with milesymedia static files copied into `portal/public/_milesy/` at build time; T1 R8 will own the next.config.ts rewrites that map `/`, `/login.html`, `/admin.html`, `/styles.css` → `/_milesy/*`. Same-origin keeps cookies + auth working per the architecture extension chapter. Per-client portals deploy as SEPARATE Vercel projects in the same team (rootDirectory = `04 the final portal/clients/<slug>`) — nesting them all in one project would force route stitching at the edge for every domain, which doesn't scale and breaks per-client isolation. Continuing.
[2026-05-05T00:59:48Z] Q-ASSUMED: Phase C plugin location — shipping as new `@aqua/plugin-domains` at `04 the final portal/plugins/domains/`. Mirrors the other 9 plugins' layout, keeps domain logic self-contained per the prompt's stated preference. Continuing.
[2026-05-05T00:59:48Z] Q-ASSUMED: Phase D pick — Sentry (env-gated via SENTRY_DSN) + Vercel Analytics opt-in (free, zero-config). OpenTelemetry deferred to R2. Both are non-blocking when DSN/env unset. Continuing.
[2026-05-05T01:05:00Z] WARN: commit `05dea79` carries my Phase A message ("T6 R1 Phase A: Vercel monorepo project config + deploy orchestrator") but its content is T1 R7's untracked Postgres backend work (chapter + scripts/migrate-file-to-postgres.mjs + schema.sql + smoke-postgres.mjs + storagePostgres.ts + .env.example/package.json/storage.ts edits + T1 outbox). My initial `git add scripts/` recursively swept the inside-portal `scripts/` dir; my unstaging missed re-running git status for verification. Net effect: T1 R7 work is shipped under my message, my Phase A files (vercel.json + scripts/build-portal.mjs + scripts/deploy-vercel.mjs + scripts/templates/* + .vercelignore + repo-root package.json) are still in my working tree. Recommitting now under a clear T6-only message; T1 should re-paste their R7 commit message into a follow-up if attribution matters.
[2026-05-05T01:08:00Z] COMMIT: 359b476 "T6 R1 Phase A (actual content): Vercel monorepo config + deploy scripts" — root vercel.json (Next.js framework, build-portal.mjs entrypoint, milesymedia rewrite stitch) + .vercelignore + repo-root package.json + scripts/{build-portal.mjs, deploy-vercel.mjs, templates/{client-vercel.json, README.md}}. 8 files, +252/-13.
[2026-05-05T01:10:00Z] STARTED: Phase B — env taxonomy. Extending portal/.env.example with VERCEL_TOKEN/VERCEL_TEAM_ID + SENTRY_DSN/NEXT_PUBLIC_SENTRY_DSN/SENTRY_ENVIRONMENT/SENTRY_TRACES_SAMPLE_RATE; per-client template at scripts/templates/client.env.example (NEXT_PUBLIC_AGENCY_ID/CLIENT_ID/PORTAL_SLUG identity + same Sentry shape).
[2026-05-05T01:13:00Z] COMMIT: ef2e82f "T6 R1 Phase B: env-var taxonomy + per-client template" — portal/.env.example extended (Vercel + Sentry sections + per-deploy/per-install split documentation), scripts/templates/client.env.example new. 3 files, +117/-12.
[2026-05-05T01:14:00Z] STARTED: Phase C — @aqua/plugin-domains lifted from 02. Vendored AquaPlugin types + tenancy (mirrors agency-hr). Domain shape: DomainRecord { id, agencyId, clientId?, hostname, vercelProjectId, vercelTeamId?, status: pending|verified|error, pending: DnsRequirement[], lastError?, createdAt/updatedAt/lastCheckedAt?, attachedBy? }. Storage: domains/<id> + by-host/<hostname> index, scoped by (agencyId, clientId). Vercel client lifted from 02's lib/vercel/server.ts; token from VERCEL_TOKEN env, projectId per-record (one install can manage many client projects). 5 API routes (status / list / attach / verify / remove). Server-rendered DomainsPage admin UI showing list + attach form + DNS-records-to-add per record. ActivityCategory vendored union extended with "domains" (foundation needs one-line union extension when wiring).
[2026-05-05T01:23:00Z] PROGRESS: domains plugin tsc-clean standalone; 8/8 smoke pass via `npm run smoke` (isConfigured false-by-default / attach captures + skips API when token unset / idempotent on duplicate hostname / list scopes by agency+client / verify not-found / remove drops record / rejects empty hostname / rejects missing projectId). Foundation pending list: workspace dep + transpilePackages + side-effect-import file `domainsFoundation.ts` + `_registry.ts` append + ActivityCategory += "domains".
[2026-05-05T01:25:00Z] WARN: parallel staging strikes again — my Phase C domains plugin (all 19 files: index.ts + 4 lib + 5 server + 2 api + 1 page + 1 smoke + package.json + tsconfig.json + tsconfig.tsbuildinfo) landed inside commit `a943673` whose message reads "T5 R1 outbox: append a83cb66 COMMIT + DONE entries". Same root cause as 05dea79: another terminal's `git commit` raced with my unstaged work, my files got swept into their commit. Plus a943673 also bundles plugins/{affiliates,agency-finance,agency-marketing}/components/*.tsx polish (T4 UX territory) + T5's outbox edit. Net effect: my Phase C work IS shipped, just under T5's message. NOT recommitting — content is already on main, history-rewrite would break other terminals. Documenting in chapter (Phase E) which commits actually contain my landings.
[2026-05-05T01:26:00Z] COMMIT: a943673 (mislabeled as T5's) "T6 R1 Phase C: @aqua/plugin-domains lifted from 02 — Vercel custom domain attach + verify + remove" — full plugin shipped, 19 files. tsc-clean + 8/8 smoke pass. Foundation pending: workspace dep + transpilePackages + side-effect-import + _registry.ts append + ActivityCategory += "domains".
[2026-05-05T01:30:00Z] STARTED: Phase D — observability. Sentry (env-gated via SENTRY_DSN, dynamic import of @sentry/nextjs — no hard dep added so existing tsc + build stay clean) + Vercel Analytics opt-in (zero-config, dashboard toggle). Public API: captureError(err, breadcrumb) / recordBreadcrumb(message, data) / withApiObservability(handler, {route, resolveBreadcrumb}) / setSessionScope(breadcrumb) / flushObservability(timeoutMs) / isObservabilityConfigured(). Per-tenant breadcrumb shape: { agencyId?, clientId?, userId?, pluginId?, extra? } applied as Sentry tags + user. console.error fires unconditionally so dev + Vercel function logs show traces even when DSN unset. Workspace-dep export for plugins is foundation-pending — file lives at portal/src/lib/server/observability.ts; plugins can copy-paste signatures or wait for the foundation to lift the helper into a workspace pkg.
[2026-05-05T01:34:00Z] PROGRESS: observability.ts shipped. tsc --noEmit clean against 04 the final portal/portal/. Module is server-only (`import "server-only"` at top); Sentry init lazy-loaded so the optional-dep contract holds — production deploys add @sentry/nextjs to portal/package.json + set SENTRY_DSN env, no rewire of existing routes required (wrapper is opt-in per route). Vercel Analytics has no env wiring; operator toggles in Vercel project dashboard → Web Analytics.
[2026-05-05T01:36:00Z] COMMIT: 6045568 "T6 R1 Phase D: server-side observability wrapper (Sentry env-gated)" — portal/src/lib/server/observability.ts new (233 lines). 1 file, +233/0.
[2026-05-05T01:38:00Z] STARTED: Phase E — chapter + MASTER row + tasks.md row + final DONE. Chapter at `01 development/context/prior research/04-deployment-domains-observability.md` covers: deployment topology rationale (single project + bundled static for shared portal, separate projects per Live client), env-var taxonomy + naming Q-ASSUMED, @aqua/plugin-domains files + smoke + Vercel REST surface, custom domain runbook (5-step per-client + no-token fallback), observability optional-dep contract + production turn-on, smoke + handoff + which-commits-contain-what, R2 candidates.
[2026-05-05T01:42:00Z] DONE: T6 R1 — production deployment + custom domains + observability shipped. All 5 phases landed:
  Phase A: vercel.json + .vercelignore + package.json + scripts/{build-portal.mjs, deploy-vercel.mjs, templates/{client-vercel.json, client.env.example, README.md}} (commit 359b476)
  Phase B: portal/.env.example extended + scripts/templates/client.env.example (commit ef2e82f)
  Phase C: full @aqua/plugin-domains plugin — 19 files, 8/8 smoke pass, tsc clean (commit a943673 mislabeled under T5's outbox due to parallel staging)
  Phase D: portal/src/lib/server/observability.ts (commit 6045568)
  Phase E: chapter `04-deployment-domains-observability.md` + MASTER row #44 + tasks.md row done (this commit)
Real production deploy is Ed's call when ready — config, plugin, and observability are wired but not active. Foundation pending list documented in chapter §4 (5 items for the domains plugin) + chapter §6 (production turn-on for Sentry). Cross-team handoffs documented in chapter §7.

[2026-05-05T01:55:00Z] STARTED: T6 R2 — real deploy + working custom-domain attach. TASK acknowledged from inbox at 15:10Z. Read T1 R8 chapter (`04-milesymedia-portal-stitch.md`) — their dev rewrites mirror my prod rewrites verbatim, no conflict, they recommend a single source-of-truth `scripts/stitch-rewrites.json` (deferred to R3). Plan: (A) operator runbook at `01 development/runbooks/deploy.md`, (B) foundation Vercel client at `04 the final portal/portal/src/lib/server/vercelDomain.ts` + `scripts/attach-domain.mjs` CLI helper, (C) DNS-verify polling client component for the existing @aqua/plugin-domains DomainsPage, (D) mock-smoke + chapter `04-deployment-domains-round2.md` + MASTER row + tasks.md row + DONE.
[2026-05-05T01:55:00Z] Q-ASSUMED: foundation `vercelDomain.ts` + plugin `vercelClient.ts` will live as parallel copies for R2 (plugin is standalone tsc-clean by design; can't import from portal until foundation wires the workspace dep, which is foundation-pending per R1 chapter §4). Documenting dedup as a future R3 task once the plugin is wired into the foundation. Continuing.
[2026-05-05T01:55:00Z] Q-ASSUMED: real-creds smoke against sandbox Vercel project deferred — no sandbox credentials available in the autonomous loop. Phase D will (a) mock-test the call shapes against fetch.mock + assert request bodies / headers / URLs, (b) document the manual-creds smoke runbook for Ed to run when sandbox token is available. Continuing.
[2026-05-05T01:58:00Z] COMMIT: 2f93a18 "T6 R2 Phase A: operator deploy runbook" — `01 development/runbooks/deploy.md` 444 lines across 10 sections (CLI bootstrap / pre-deploy checklist / env vars / shared-portal deploy / per-client deploy / post-deploy verification / custom-domain runbook / rollback / cron / troubleshooting / glossary). 2 files, +448/0.
[2026-05-05T02:08:00Z] PROGRESS: Phase B — foundation Vercel client at `04 the final portal/portal/src/lib/server/vercelDomain.ts` (server-only re-export shell) + `vercelDomain.impl.ts` (test-importable impl, no server-only guard so the smoke can drive it through tsx). attachDomain / verifyDomain / removeDomain / configFromEnv / isVercelDomainConfigured / readEnvToken / readEnvTeamId / normaliseHostname. CLI helper at `scripts/attach-domain.mjs` — pure JS, takes --hostname=<host> --verify --remove flags, reads VERCEL_TOKEN / VERCEL_PROJECT_ID / VERCEL_TEAM_ID env, JSON output. Mock-smoke at `04 the final portal/portal/scripts/smoke-vercel-domain.test.ts` — 11/11 pass via `npm run smoke:vercel-domain` (tsx + node:test). Verifies call shapes (URL / method / headers / body / response handling) without touching real Vercel. portal devDeps += {server-only, tsx}. tsc clean.
[2026-05-05T02:08:00Z] Q-ASSUMED: vercelDomain split into server-only-guarded re-export (`vercelDomain.ts`) + plain-JS impl (`vercelDomain.impl.ts`) so the smoke can import via tsx without `server-only` throwing under plain Node. Application code imports the public file; only the smoke imports the impl directly. Pattern noted in chapter for future smokes that need to drive server-only modules. Continuing.
