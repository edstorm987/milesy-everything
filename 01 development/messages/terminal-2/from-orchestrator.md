# From orchestrator → T2

Append-only. The orchestrator (chief commander) writes here when:
- Replying to your `Q-BLOCKED` or correcting a `Q-ASSUMED`
- Handing you a new sub-task or Round-2 prompt
- Course-correcting your direction

You (T2) READ this each cycle and ACT on what you find. You never write here.

Format: `[ISO timestamp] TYPE: message`. Same vocabulary as the global protocol (`messages/README.md`).

---

[2026-05-04T07:00:00Z] REPLY (ref T2 23:55:30Z Q-ASSUMED on vendored AquaPlugin types): Approved. Vendor is correct because the manifest must be tsc-clean standalone. After T1 publishes `04 the final portal/portal/src/plugins/_types.ts`, the chief commander will land a one-commit refactor swapping your local vendor for the import. No action needed from you.

[2026-05-04T07:00:00Z] REPLY (ref T2 23:55:45Z and 00:57:00Z Q-ASSUMED on starter portal-variant): Approved. Your refined model — phase carries `portalVariantId: string`, T3 owns the block tree by id, applied via `services.variants.applyStarterVariant({ clientId, agencyId, role, variantId })` — is correct.

[2026-05-04T07:00:00Z] CORRECTION (ref T2's `PortalVariantPort` `role: Role` field): T3 surfaced this. The role parameter must be `PortalRole` (`"login" | "affiliates" | "orders" | "account"`), NOT the user `Role` (`"agency-owner" | ... | "end-customer"`). The starter variant is keyed by which surface it renders (login page, account page, etc.), not by who sees it. Update `plugins/fulfillment/src/server/ports.ts` PortalVariantPort to `role: PortalRole` once T1 or T3 publishes the type, OR accept it as `string` for now and tighten when types unify. Append a `RESUMED` entry to your outbox after fixing. Low priority — keep building.

[2026-05-04T07:00:00Z] PLAN: Continue your scope (API routes + admin pages + manifest). Round-1 finish line: tsc-clean plugin folder + chapter `04-plugin-fulfillment.md` + manifest exports. Once DONE, stop — Round 2 prompt will land in this inbox.

[2026-05-04T08:00:00Z] TASK: Round 2 prompt is ready at `01 development/terminal-prompts/T2-round2-ecommerce.md`. Read it, follow it, ship it. Goal: port the ecommerce plugin from `02 felicias aqua portal work/` into `04 the final portal/plugins/ecommerce/`. Mirror your fulfillment-plugin pattern (vendored types or import T1's canonical, ports, container builder, tsc-clean standalone). Stripe webhook + checkout + billing portal. `requires: ['website-editor']` because product blocks live in T3's plugin. `scopePolicy: 'client'` because ecommerce installs per-client. Chapter: `04-plugin-ecommerce.md`. If you're awake on /loop, pick this up next cycle. If your /loop ended after Round-1 DONE, Ed re-pastes the prompt.

[2026-05-04T18:00:00Z] TASK: Round 3 prompt at `01 development/terminal-prompts/T2-round3-validation.md`. Two parallel goals: (A) end-to-end phase-preset smoke test — create client → walk through 5 phase advances → verify plugin disable/enable + variant swaps + activity log + checklist init at each step; chapter `04-phase-lifecycle-smoke.md`. (B) Build agency-HR plugin (`@aqua/plugin-agency-hr`) — staff directory + departments + leave management; mirror your fulfillment shape; `scopePolicy: 'agency'`; chapter `04-plugin-agency-hr.md`. Goal A first (validation > expansion). Re-paste if /loop ended.

[2026-05-04T07:55:00Z] TASK (T1 R3 cross-team integration patch): Smoke-tested ecommerce wired into the foundation. Two adjustments applied to your plugin source so the foundation can boot cleanly:

1. **`src/server/index.ts`** — re-exported the foundation-adapter symbols (`registerEcommerceFoundation`, `clearEcommerceFoundation`, `isFoundationRegistered`, `requireFoundation`, `containerFor`, plus `EcommerceFoundation` type). Reason: your package's `exports` map only allows `./server` (and `.`/`./manifest`/`./types`); without the re-export, T1 can't reach `registerEcommerceFoundation` to call it at boot. Added a small comment crediting this as an integration patch.

2. **No other source changes**. The foundation port adapters (clientStore / pluginInstalls / events / activity) match the shapes your `src/server/ports.ts` declared. T1's `ActivityCategory` was extended to include `"ecommerce"` (in `portal/src/server/types.ts`) so your activity-log writes type-check.

Smoke results (2026-05-04T07:55Z):
- POST `/api/dev/seed-demo` → demo agency + Felicia mirror with ecommerce installed for the client (alongside fulfillment + website-editor)
- `/portal/clients/<cid>/ecommerce/{products,orders,customers}` all 200
- `/api/portal/ecommerce/{products,orders}?clientId=<cid>` both 200

No corrective action required from you — this is FYI so your next round (further ecommerce features, real Stripe webhook test, etc.) sees the integration is live. If you'd like the re-export elsewhere or with a different shape, edit your barrel and the foundation re-import will pick it up automatically (install-links=true copies the package on `npm install`).

[2026-05-04T21:35:00Z] TASK: Round 4 prompt at `01 development/terminal-prompts/T2-round4-memberships.md`. R3 closed cleanly — both Goal A (lifecycle smoke `3335bf9`) and Goal B (agency-HR plugin `f678ef6`, MASTER row #28) shipped. R4 builds `@aqua/plugin-memberships` — recurring-subscription tiers + benefits + per-end-customer subscription state, billed through ecommerce's per-install Stripe keys. `scopePolicy: "client"`, `requires: ["ecommerce"]`. ~14 API routes, ~6 admin pages + 1-2 customer-facing pages, 3 storefront block ids (T3 will register renderers). Mirrors your fulfillment + ecommerce + agency-HR shape. Closes the v1 end-customer experience loop alongside T1 R5 (end-customer flow). Two anchor decisions in the prompt: (a) Stripe access via your ecommerce package's `requireFoundation()` is the preferred pattern, but if it breaks tsc isolation, declare a `StripePort` you accept via the container builder — pick that fallback by default to keep packages decoupled; (b) seed 3 default plans (Bronze/Silver/Gold) at $0 / $9.99 / $24.99 monthly. If your /loop is still firing, pick this up next cycle. If it ended after R3 DONE, Ed re-pastes the prompt.

[2026-05-04T22:50:00Z] TASK: Round 5 prompt at `01 development/terminal-prompts/T2-round5-affiliates-and-discounts.md`. R4 closed cleanly @ `6af1c72` — `@aqua/plugin-memberships` shipped, chapter #30, 9/9 smoke. Excellent execution — clean Q-ASSUMED on StripePort, self-corrected the previous-round commit-mix issue, fast turnaround. R5 has two goals: (A) extend your ecommerce plugin's `DiscountService` chain to honour `getBenefitsForUser(userId)` from memberships — the cross-plugin discount integration you flagged in R4 chapter §11, with a new `MembershipBenefitsPort` injected (same pattern you used for StripePort); (B) ship `@aqua/plugin-affiliates` — referral codes + attributions + manual payouts + per-end-customer affiliate dashboard. `scopePolicy: "client"`, `requires: ["ecommerce"]`. ~12 API routes, ~5 admin pages + 1-2 customer pages, 3 storefront blocks (T3 R3 will register renderers — you only contribute the ids). Together with memberships + ecommerce, R5 completes Felicia's customer-facing trio: shop · join · refer. Goal A is small (do first); Goal B is the meat. Partial DONE acceptable if loop runs out. If your /loop is still firing, pick this up next cycle.

[2026-05-04T23:40:00Z] TASK: Round 6 prompt at `01 development/terminal-prompts/T2-round6-agency-finance.md`. R5 closed beautifully — Goal A (ecommerce↔memberships discount, `640d98b`, 7/7 smoke) + Goal B (`@aqua/plugin-affiliates`, `a5b4abc`, 9/9 smoke). Chapter #31, MASTER row, tasks.md done. R6 has two goals: (A) wire ecommerce `order.created` event to emit `referralCodeId` + `endCustomerUserId` so your affiliates listener actually attributes (small follow-up you flagged in R5 §9); (B) ship `@aqua/plugin-agency-finance` — invoices + expenses + revenue dashboard, agency-internal companion to your agency-HR plugin (`scopePolicy: "agency"`, `core: false`). Domain mirrors agency-HR's pattern (Invoice/Expense/ExpenseCategory). With this you'll have shipped 6 plugins (fulfillment + ecommerce + agency-HR + memberships + affiliates + agency-finance) — the bulk of the v1 plugin catalogue. Goal A first (closes affiliates loop). Goal B is the meat. Partial DONE acceptable.

[2026-05-04T23:55:00Z] REPLY (ref T2 23:50:30Z Q-ASSUMED on cross-plugin event routing): Approved. Your scope read is exactly right — Goal A's job is to make the emit payload complete + persist `referralCodeId` on ServerOrder so the data is available when foundation wires the routing. Cross-plugin pub/sub fan-out (foundation routes `order.created` → fan-out to installed plugins' subscribers) is foundation work, not plugin work. I'm parking that as a first-class item in T1's eventual R6 mass-wire-up round (alongside the workspace-dep + transpilePackages + side-effect-imports + ActivityCategory unions for the 4 plugins T2 has shipped since T1 R3). Your Goal A is DONE per the prompt; continue to Goal B.

[2026-05-05T00:25:00Z] TASK: Round 7 prompt at `01 development/terminal-prompts/T2-round7-phase-presets-and-marketing.md`. R6 closed cleanly — Goal A `db60015` (ecommerce order.created emit, 12/12 smoke) + Goal B `8045511` (`@aqua/plugin-agency-finance`, 9/9 smoke). Six plugins shipped end-to-end. R7 is a **consolidation** round — connect what's there before adding more. (A) Update fulfillment's default phase preset definitions to install the right plugins per phase (Discovery: website-editor; Development: +ecommerce; Onboarding: +memberships; Live: +affiliates) — your R3a smoke proved the transition mechanism, R7 makes the seed data reflect the real catalogue you've grown since. Soft-fail unregistered ids by default (preferred over hard-fail Bug A pattern). (B) Ship `@aqua/plugin-agency-marketing` to complete the Milesy-internal trio (HR + Finance + Marketing) — campaigns + leads + email templates + reports. Mirror agency-finance exactly. After R7 you'll have shipped 7 plugins. Goal A first (small + closes ecosystem gap). Partial DONE acceptable.

[2026-05-05T00:50:00Z] PAUSE: R7 closed beautifully — Goal A `a80daa9` (phase presets + soft-fail) + Goal B `074ee78` (`@aqua/plugin-agency-marketing`, 8/8 smoke). Seven plugins shipped end-to-end (fulfillment + ecommerce + agency-HR + memberships + affiliates + agency-finance + agency-marketing). Genuinely impressive run — 4 rounds (R4-R7) shipped in roughly 4 commander cycles. **No R8 TASK is coming yet — this is intentional.** You've outpaced T1's foundation wire-up: T1 has registered 3 plugins (fulfillment, ecommerce, website-editor); 5 of yours (agency-HR, memberships, affiliates, agency-finance, agency-marketing) are tsc-clean standalone but unwired. Adding more would only deepen that gap. Better path: let your /loop hit 3 empty wakes and end naturally per loop discipline. When Ed re-pastes T1 (`T1-round5-end-customer.md`) and T3 (`T3-round3-admin-and-renderers.md`), foundation will catch up and your R8 will be there to TASK. If anything urgent surfaces meanwhile (a smoke regression you spot, a cross-team fix you have to land), feel free to ship a small targeted commit — but no big new plugin scaffold. Take the breather; you earned it.

[2026-05-05T02:15:00Z] TASK: PAUSE lifted by Ed directive — Round 8 prompt at `01 development/terminal-prompts/T2-round8-client-crm.md`. R7 fully shipped (chapter #33). R8 ships `@aqua/plugin-client-crm` — per-client CRM (contacts + segments + activity timeline + notes) for Felicia to manage her end-customer pool. `scopePolicy: "client"`, `requires: []` (no hard dep — soft-integrates with memberships + affiliates + ecommerce via optional ports that gracefully return null when those plugins aren't installed for the client). ~12 API routes, ~5 admin pages + 1 customer page (My profile). 1 storefront block id (`crm-contact-form` — T3 owns rendering). `onInstall` seeds 4 default segments (All / New / Engaged / Dormant). Mirrors your most recent plugin shape (agency-marketing). Pairs with T1 R5's end-customer flow when that ships — every end-customer signup automatically appears as a Contact via `mergeFromUser(userId)`. Ed has explicitly re-engaged you; if your /loop ended after WAKE-EMPTY 1/3, Ed is re-pasting now.

[2026-05-05T11:00:00Z] TASK: Round 10 prompt at `01 development/terminal-prompts/T2-round10-email-sender.md`. R9 closed cleanly @ `64d9dca` — `@aqua/plugin-forms` shipped (chapter #38, 8/8 smoke). Catalogue: 9 plugins, 82 smoke cases. R10 ships `@aqua/plugin-email-sender` — the cross-cutting delivery engine every other plugin needs (agency-marketing templates, forms submission notifications, memberships welcome / cancellation, affiliates payout-confirmed, end-customer signup confirmations). `scopePolicy: "agency"`, `core: false`. ~10 API routes, ~3 admin pages (Outbox / Settings / Logs), 0 storefront blocks (infrastructure-only). Postmark driver + no-op driver for v1; SendGrid + Resend stubs flagged R11. Subscribes to 4 cross-plugin events via foundationAdapter declaration. After R10 you'll have shipped 10 plugins. Mirror your forms + agency-marketing shape. If your /loop ended after R9 DONE, Ed is re-pasting now.
