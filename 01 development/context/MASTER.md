# Context — Master Index

This folder is the project's persistent memory, organised as a **context
tree** (fractal book metaphor):

```
context/
├── MASTER.md          ← the contents page (you are here)
├── prior research/    ← chapter files mapping `02` and `03` codebases
└── *.md               ← future top-level chapter files
```

`MASTER.md` is the table of contents. Every chapter on the shelf gets a row
here with a one-line summary. Each chapter is its own markdown file that
goes deep on a single topic.

---

## How it works

### Reading (recall)
1. Open `MASTER.md` (this file).
2. Scan the chapter table for the topic you need.
3. Open the corresponding chapter file for the full detail.

### Writing (a new learning)
1. Decide which topic the new knowledge belongs to.
2. Either:
   - **Existing topic** → open the chapter, add to it. Update the row's
     one-line summary if it shifted.
   - **New topic** → create a new file `chapter-name.md` AND add a new row.
3. Keep the table sorted; pick the next free number.

### Naming
- Lowercase-kebab-case filenames (`aqua-plugin-system.md`).
- One topic per chapter. Split when a chapter exceeds ~300 lines or covers
  two distinct things.

### Granularity rule of thumb
A chapter is right-sized when one paragraph in `MASTER.md` can't capture
what's in it.

---

## Chapters — Prior research (Phase 0)

Mapping of `02 felicias aqua portal work/` and `03 old portal/` codebases.
Synthesised from 6 parallel explore agents.

### From `02 felicias aqua portal work/` (Aqua / Felicia portal)

| # | Title | File | Summary |
|---|-------|------|---------|
| 01 | Plugin system | [prior research/aqua-plugin-system.md](prior%20research/aqua-plugin-system.md) | 34 plugins, contract types, runtime install/uninstall flow, 16 presets, path mapping, validator. |
| 02 | Server modules | [prior research/aqua-server-modules.md](prior%20research/aqua-server-modules.md) | 41 domain modules. Storage abstraction, eventBus, orgs, email, webhooks. The five most architecturally critical. |
| 03 | Admin routes | [prior research/aqua-admin-routes.md](prior%20research/aqua-admin-routes.md) | ~70 admin destinations grouped by domain. Plugin contributions + shared chrome (AdminTabs / PluginPageScaffold / SetupChecklist / Ask Aqua). |
| 04 | Aqua dashboard routes | [prior research/aqua-aqua-routes.md](prior%20research/aqua-aqua-routes.md) | Cross-client agency surface — `/aqua` + `/aqua/[orgId]/marketplace` + new-portal flow + support hub. |
| 05 | API surface | [prior research/aqua-api-routes.md](prior%20research/aqua-api-routes.md) | Every HTTP endpoint by area: `/api/auth`, `/api/admin`, `/api/portal/*`, `/api/stripe`, `/api/og`. |
| 06 | Visual editor | [prior research/aqua-visual-editor.md](prior%20research/aqua-visual-editor.md) | Live · Block · Code modes. Simple / Full / Pro complexity. Outliner + properties + topbar. Publish + GitHub PR flow. |
| 07 | Block library | [prior research/aqua-blocks.md](prior%20research/aqua-blocks.md) | 58 storefront blocks catalogue. BlockRenderer mechanics (split-test, theme overlay, responsive CSS, animations). |
| 08 | Portal variants | [prior research/aqua-portal-variants.md](prior%20research/aqua-portal-variants.md) | `PortalRole` + `isActivePortal` singleton. Server helpers. Admin UI. Starter trees. Customer-facing safety fallback. |
| 09 | Storefront | [prior research/aqua-storefront.md](prior%20research/aqua-storefront.md) | Public routes, customer components (Navbar, Footer, ProductDetail, CartDrawer, ChatBot, …), CartContext. |
| 10 | Auth + middleware + admin libs | [prior research/aqua-auth-middleware.md](prior%20research/aqua-auth-middleware.md) | Sessions (HMAC), scrypt, rate limits, CSP, dev-bypass modes, `/api/auth/*` endpoints, force-password-change, `src/lib/admin/` helpers. |

### From `03 old portal/` (Aqua Portal v9 archive)

| # | Title | File | Summary |
|---|-------|------|---------|
| 11 | Overview | [prior research/old-portal-overview.md](prior%20research/old-portal-overview.md) | 7-app monorepo, Phase-9 plugin migration, how to run, archive state. 70% architecturally sound, 40% functionally complete. |
| 12 | Bridge package | [prior research/old-portal-bridge.md](prior%20research/old-portal-bridge.md) | `@aqua/bridge` shared package. Types, auth, Prisma schema, eventBus, registry, sync helpers, UI kit (480 LOC), postMessage protocol, concepts/. |
| 13 | Host shell | [prior research/old-portal-host-shell.md](prior%20research/old-portal-host-shell.md) | `aqua-host-shell` — bootstrap loader, tolerant view resolver, sidebar/topbar, marketplace, AI chat (Claude Opus 4.7 SSE + prompt cache). |
| 14 | Sub-apps | [prior research/old-portal-suites.md](prior%20research/old-portal-suites.md) | The 6 sub-apps (Client / CRM / Finance / People / Revenue / Operations). What's wired vs stubbed per template. |
| 15 | Roles + multi-tenancy | [prior research/old-portal-roles-tenancy.md](prior%20research/old-portal-roles-tenancy.md) | 6 roles, Prisma models (Agency, User, Client, FulfilmentBrief, etc.), ClientStage, sync helpers. The most directly applicable to `04`. |
| 16 | Extras folder | [prior research/old-portal-extras.md](prior%20research/old-portal-extras.md) | `vite-prototype/` and `eds-old-portal-idea-fixed/` reference patterns (PageBuilder / RoleBuilder / AgencyConfigurator / collaboration widgets / DynamicRenderer). `sort-out-version/` is delete-safe. |

### Synthesis

| # | Title | File | Summary |
|---|-------|------|---------|
| 17 | Concepts to port | [prior research/concepts-to-port.md](prior%20research/concepts-to-port.md) | Ranked list: from `02` (port directly), from `03` (recreate as plugins). Architecture target for `04`. |
| 18 | Anti-patterns | [prior research/anti-patterns.md](prior%20research/anti-patterns.md) | 20 specific things from `02` and `03` we should NOT replicate in `04`. |

### Architecture (LOCKED — read before any 04 work)

| # | Title | File | Summary |
|---|-------|------|---------|
| 19 | `04` architecture | [prior research/04-architecture.md](prior%20research/04-architecture.md) | The locked design for `04 the final portal/`. Pool-model multi-tenancy (Agency → Client → End-customer), Aqua manifest plugins, server-rendered chrome, single-cookie auth, phase lifecycle, brand kit per client. Round 1 terminal split. Decisions log. |
| 19b | `04` architecture extension — per-client portals + milesymedia stitch | [prior research/04-architecture-extension-per-client-portals.md](prior%20research/04-architecture-extension-per-client-portals.md) | Operating reality refines v1: milesymedia.com is the front door; Aqua portal is shared for pre-Live work + is the editor/generator for Live clients' custom portals. Each Live client gets `clients/<slug>/` as a separate Next.js app generated via "Export to repo" + edited via the same web editor. Iframe-embedded into client domains for performance; same auth origin. Preset portal library. |

### Round 1 implementation chapters

| # | Title | File | Summary |
|---|-------|------|---------|
| 20 | Fulfillment plugin | [prior research/04-plugin-fulfillment.md](prior%20research/04-plugin-fulfillment.md) | T2's deliverable. Manifest contract, six seeded phase defaults, `advancePhase` 7-step transition algorithm, collaborative checklist (template + per-client progress), per-client marketplace UX, foundation port surface (T1 + T3 integration TODOs), API surface. Standalone tsc-clean. |
| 21 | Foundation (T1) | [prior research/04-foundation.md](prior%20research/04-foundation.md) | T1's deliverable. Folder tree of `04 the final portal/portal/`, plugin contract (AquaPlugin manifest + new `scopePolicy` field + `:clientId` href rewriting), auth API (issueSession/requireRole/requireRoleForClient/AuthError), chrome contract (buildSidebar + ThemeInjector + Sidebar/Topbar), server module surface (tenants/users/pluginInstalls — composite install id `${agencyId}\|${clientId??_agency}\|${pluginId}`), bootstrap flow, six deviations from architecture. |
| 22 | Website-editor plugin (T3) | [prior research/04-plugin-website-editor.md](prior%20research/04-plugin-website-editor.md) | T3's deliverable. `@aqua/plugin-website-editor` package — manifest (8 navItems / 11 pages / 41 api / 58 blocks / 8 features), public exports (./manifest, /server, /types, /components), `applyStarterVariant({agencyId, clientId, role: PortalRole, variantId, actor?}, storage)` contract for T2, 6 starter JSON trees, full server runtime (pages.ts variant helpers + themes/content/sites/embeds/preview), storage-keys namespacing under `t/{agencyId}/{clientId}/...`, smoke 31/31 pass, tsc clean. Round-2 TODO list: block UI port, admin UI port, split-test wiring, GitHub PR promote, custom domains, theme-token-system, real-time collab. |
| 23 | Foundation Round 2 (T1) | [prior research/04-foundation-round2.md](prior%20research/04-foundation-round2.md) | T1's Round-2 wire-and-demo. `@aqua/plugin-fulfillment` mounted as `file:..` workspace dep with `install-links=true` + `transpilePackages`; foundation port adapters (`clientStore`, `pluginInstalls`, `pluginRuntime`, `pluginRegistry`, `phaseStore`, `activityLog`, `eventBus`, `portalVariant` STUB) bridge T2's ports to T1 server modules; `_routeResolver.ts` + 3 catch-all routes (`/portal/agency/[...rest]`, `/portal/clients/[clientId]/[...rest]`, `/api/portal/[plugin]/[...rest]`) dispatch URLs to manifest pages + handlers; agency creation auto-installs core plugins via `bootstrapAgency`; `/api/dev/seed-demo` provisions Demo · Aqua + Felicia mirror with onboarding-stage half-ticked checklist. Smoke flow green end-to-end. |

### Round 2 implementation chapters

| # | Title | File | Summary |
|---|-------|------|---------|
| 24 | Ecommerce plugin (T2) | [prior research/04-plugin-ecommerce.md](prior%20research/04-plugin-ecommerce.md) | T2's Round-2 deliverable. `@aqua/plugin-ecommerce` (`scopePolicy: "client"`, `requires: ["website-editor"]`). Server domain: OrderService (Stripe-webhook-idempotent upserts), ProductService (per-install CRUD, override + inventory-snapshot merge), GiftCardService, ReferralCodeService, DiscountService (gift card → referral → static promo → custom code). Per-install Stripe (no env vars) — keys read from `install.config`. 23 API routes including `/stripe/{checkout,webhook,billing-portal}`. 13 admin pages (Products + Variants + Collections + Orders + Receipt + Customers + Inventory + Shipping + Discounts). Storefront cart context + 7 client components. 8 block ids contributed (rendering delegated to T3). Foundation adapter pattern: `registerEcommerceFoundation` + `containerFor(storage)` per request. Vestigial `billing.ts` flagged. tsc-clean standalone. |

### Round 3 implementation chapters

| # | Title | File | Summary |
|---|-------|------|---------|
| 25 | Foundation Round 3 (T1) | [prior research/04-foundation-round3.md](prior%20research/04-foundation-round3.md) | T1's Round-3 three-plugins-live. Workspace deps for ecommerce + website-editor + fulfillment. `_routeResolver.ts` extended to handle two manifest path conventions (relative for T2/ecommerce, full-URL for T3) with both `:name` and `[name]` placeholders. Real `portalVariantAdapter` wraps T3's `applyStarterVariant` with the website-editor plugin's storage. `ecommerceFoundation.ts` side-effect-import registers `EcommerceFoundation` at boot via the adapter pattern. Cross-team patch: re-exported `registerEcommerceFoundation` from T2's `src/server/index.ts` so `exports`-map permits it. `ActivityCategory` extended with `"ecommerce"`. Demo seed installs both client-scoped plugins on Felicia. Smoke green: 14 page URLs all 200, fulfillment+ecommerce+website-editor APIs all dispatch correctly. |
| 26 | Phase lifecycle smoke (T2 R3) | [prior research/04-phase-lifecycle-smoke.md](prior%20research/04-phase-lifecycle-smoke.md) | T2's Round-3 end-to-end validation. Two smoke harnesses under `plugins/fulfillment/src/__smoke__/`: in-process `lifecycle.test.ts` (9 `node:test` tests, mocks all 8 foundation ports, walks `seedPhases → createWithPhase → tick → advance ×4`) and HTTP `lifecycle.http.mjs` (~50 assertions against a live `npm run dev`, hits seed-demo + login + clients + checklist/tick + phase/advance + activity + marketplace). Surfaced + fixed Bug A: default phase presets referenced unregistered plugins (`brand`, `forms`, `email`, `analytics`, `seo`, `support`) causing 422 on advance — trimmed `DEFAULT_PHASE_PRESETS` to `website-editor` + `ecommerce` only. Bug B (variant id mismatch) is per-architecture soft-fail; logged for T3 alignment. Observation: install-link refresh requires `rm -rf node_modules/@aqua/plugin-X && npm install`. Both smokes 0 failures. `npm run smoke` script wired. |

### Round 4 implementation chapters

| # | Title | File | Summary |
|---|-------|------|---------|
| 27 | Milesy Media demo flow (T1 R4) | [prior research/04-milesymedia-demo.md](prior%20research/04-milesymedia-demo.md) | T1's Round-4 demo button. Static site (`milesymedia website/{index,login,admin}.html`) declares `<meta name="aqua-portal-base">`; an inline rewriter retargets `[data-aqua-action="sign-in"|"demo"]` hrefs at load. `SessionPayload` gains optional `isDemo` baked into the HMAC cookie. New top-level routes: `GET /demo` seeds the demo agency (idempotent) + issues an `isDemo:true` agency-owner cookie + redirects `/portal/agency`; `GET /demo/toggle` flips agency-owner ↔ client-owner using the Felicia mirror, redirects to the matching surface, anonymous/non-demo bounces to `/demo`. Seed body factored into `src/lib/server/demoSeed.ts` (shared by `/demo` and `/api/dev/seed-demo`). `resetDemo()` wipes demo agency + every descendant (clients, users, plugin installs + per-install plugin data, phases, activity entries) before re-seeding; `?reset=1` exposed on the API route (gated). `DemoBanner` is a server component injected at `/portal/layout.tsx` so the POV toggle spans both agency and client surfaces. Real `/api/auth/login` never sets `isDemo` — banner only renders for sandboxed sessions. |
| 28 | Agency-HR plugin (T2 R3) | [prior research/04-plugin-agency-hr.md](prior%20research/04-plugin-agency-hr.md) | T2's Round-3 Goal-B deliverable. `@aqua/plugin-agency-hr` at `04 the final portal/plugins/agency-hr/`. `scopePolicy: "agency"`, `core: false` (opt-in). Domain: Staff (with Role/department/manager/locationType/hourlyRate, status active|on-leave|alumni), Department (parentId tree), LeaveRequest (pto|sick|sabbatical, pending|approved|rejected with day-count from start/end). Three services (StaffService / DepartmentService / LeaveService) with cycle-safe manager + parent graphs and email-uniqueness enforcement. Four ports declared (TenantPort, ActivityLogPort, EventBusPort, PluginInstallStorePort). 13 API routes mounted at `/api/portal/agency-hr/*` with per-route `visibleToRoles`. 5 admin pages + 4 client components. Foundation adapter pattern (`registerAgencyHrFoundation` + `containerFor` singleton). `onInstall` seeds 5 default departments (Engineering / Design / Marketing / Operations / Sales). 6/6 smoke tests pass; tsc-clean standalone. Vendored `ActivityCategory` extends with `"hr"` — foundation needs a one-line union extension when wiring in. |
| 29 | Website-editor Round 2 (T3) | [prior research/04-plugin-website-editor-round2.md](prior%20research/04-plugin-website-editor-round2.md) | T3's Round-2 lift. Phase A: 58 block components ported faithfully from `02/src/components/editor/blocks/*.tsx` with `BlockRenderProps {block, editorMode, renderChildren}`; helper modules (blockStyles, AnimateOnScroll, variantResolver, themeCss, useProducts, AssetPicker, pageTemplates) lifted; BlockRenderer rewritten as 02's tree contract; blockRegistry uses 02's BlockDefinition shape with BLOCK_DESCRIPTORS derived. Phase B: visual editor admin (1,429-line `pages/EditorPage.tsx` with Live/Block/Code modes + Simple/Full/Pro tiers + outliner + properties + topbar + publish modal); canvas/ subfolder + editor admin siblings lifted. Phase C: portal-variants admin (444-line `pages/PortalsPage.tsx` with role tabs + variant CRUD + active-tab indicator). Phase D: SectionsPage / AssetsPage / PopupsPage / ThemesPage. New libs: devicePresets, splitTests, funnels, promote, sections, popup, theme rewrite, sites cache, editorMode complexity options. Shims for foundation chrome: confirm/notify/prompt/pluginRequired/AdminTabs/Tip/tabSets. Stub `ecommerceBridge.tsx` for T2 client surface (useCart/ProductVariantPicker — `setCartProvider` escape hatch). tsc clean; smoke 31/31. Round-3 deferred: PageDetailPage/CustomisePage/ThemeDetailPage/SitesPage. |
| 30 | Memberships plugin (T2 R4) | [prior research/04-plugin-memberships.md](prior%20research/04-plugin-memberships.md) | T2's Round-4 deliverable. `@aqua/plugin-memberships` at `04 the final portal/plugins/memberships/`. `scopePolicy: "client"`, `requires: ["ecommerce"]`, `core: false`. Domain: Plan (Bronze/Silver/Gold seeded — $0/$9.99/$24.99 monthly + annual variants, integer cents, ISO currency, Stripe Price ids on paid tiers); Benefit (discount/content/perk/other with optional percentOff); Subscription (per (clientId, endCustomerUserId), trialing|active|past_due|canceled|paused|incomplete, ISO currentPeriodEnd, cancelAtPeriodEnd flag). Four services: PlanService (CRUD + immutable Stripe Price sync on price change), BenefitService (CRUD + getBenefitsForUser walks subscription→plan→active benefits), SubscriptionService (subscribe/cancel/pause/resume/changePlan/upsertFromStripe/billingPortalUrl + free-tier shortcut for $0 plans), WebhookService (signature verify + storage-backed dedupe on Stripe event id + per-event-type routing). Seven ports including new StripePort (decoupled per prompt — 13 methods covering customer/subscription/checkout/billing-portal/price/webhook-verify) and new UserPort (resolve EndCustomerProfile from foundation Users). 16 API routes split admin / customer / public-webhook (`public: true` flag for catch-all bypass). 7 admin pages + 1 customer page. 3 storefront block ids (membership-paywall / membership-signup / membership-tier-grid — T3 owns rendering). `onInstall` seeds 3 default plans. tsc-clean; 9/9 smoke pass. Foundation pending: workspace dep + transpilePackages + side-effect-import file + `_registry.ts` append + `ActivityCategory` += "memberships" + UserPort projection + `stripeFor({agencyId,clientId})` reading per-install Stripe keys from the ecommerce install + catch-all `public: true` honouring. |
| 31 | Affiliates plugin + ecommerce↔memberships discount (T2 R5) | [prior research/04-plugin-affiliates.md](prior%20research/04-plugin-affiliates.md) | T2's Round-5 deliverable. **Goal A** (commit `640d98b`): extends ecommerce `DiscountService` with a 5th step keyed on `userId` — calls injected `MembershipBenefitsPort.getDiscountPercentForUser` and applies the largest membership discount, persisting `order.discountSource: "membership"` + planId snapshot. New `DiscountType: "membership"` added; `ServerOrder` gains `discountSource/discountAmount/discountCode/discountSnapshot/endCustomerUserId` with idempotent webhook-retry preservation. Backward-compatible (port absent → null). New ecommerce smoke at `src/__smoke__/discount-membership.test.ts`, 7/7 pass. **Goal B**: `@aqua/plugin-affiliates` at `04 the final portal/plugins/affiliates/`. `scopePolicy: "client"`, `requires: ["ecommerce"]`, `core: false`. Domain: Affiliate (status pending/active/suspended/removed, totalReferred + lifetimeEarnings counters), ReferralCode (upper-case unique within client, redemption counter, percent override), Attribution (idempotent on orderId, locked commissionPercentSnapshot, status pending/approved/paid/reversed), Payout (rolls approved attributions, manual markPaid, status scheduled/in_progress/completed/failed). Four services. Six ports including new EcommerceOrdersPort (cross-plugin order projection — `getOrder` reads `metadata.referralCodeId` until ecommerce ships first-class field). 16 API routes (admin + customer + internal `attributions/record` fan-out). 5 admin pages + 1 customer page (`MyAffiliatePanel` with enrol form + dashboard). 3 storefront block ids (affiliate-signup, affiliate-payout-meter, affiliate-leaderboard — T3 R3 will register renderers). Heavy use of secondary indexes (`by-user`, `by-code`, `by-order`, `by-affiliate`) for O(1) lookups. tsc-clean; 9/9 smoke pass. Foundation pending: 7 items including ecommerceOrders projection + cross-plugin event subscription routing ecommerce `order.created` → affiliates `attributions/record`. |
| 32 | Agency-finance plugin + ecommerce order.created (T2 R6) | [prior research/04-plugin-agency-finance.md](prior%20research/04-plugin-agency-finance.md) | T2's Round-6 deliverable. **Goal A** (commit `db60015`): closes the affiliates attribution loop — ecommerce now persists `referralCodeId` on `ServerOrder` + reads `metadata.referralCodeId` + `metadata.endCustomerUserId` from Stripe sessions; `upsertOrderByStripeSession` returns `{ order, isNew }` so the Stripe webhook handler emits `order.created` exactly once with the full payload (`orderId, clientId, amountTotal, currency, subtotal=amountTotal+discountAmount, referralCodeId, endCustomerUserId, discountSource`). Idempotent across webhook retries. New ecommerce smoke at `src/__smoke__/order-created-event.test.ts`, 5/5 pass. **Goal B**: `@aqua/plugin-agency-finance` at `04 the final portal/plugins/agency-finance/`. `scopePolicy: "agency"`, `core: false`. Domain Invoice (per-year sequence INV-YYYY-NNNN, line items, tax, state machine draft→sent→paid/overdue/void/refunded — markPaid is the sole path into "paid"), Expense (pending→approved/rejected→reimbursed; only-pending-editable; secondary indexes by category + staff), ExpenseCategory (six seeded defaults Salaries/Software/Travel/Marketing/Office/Other). Four services (Category, Invoice, Expense, Report). Six ports. 15 API routes. 5 admin pages including server-rendered InvoiceDetail with `renderInvoiceHtml` output. RevenueSnapshot reports trailing-window invoicesIssued/Paid/Overdue, expensesByCategory, monthly aggregate, netCents. Single-currency v1 (multi-currency consolidation deferred). tsc-clean; 9/9 smoke pass. Foundation pending: workspace dep + transpilePackages + side-effect-import + `_registry.ts` append + `ActivityCategory` += "finance" + UserPort projection (shared with memberships/affiliates). With this T2 has shipped 6 plugins (fulfillment / ecommerce / agency-HR / memberships / affiliates / agency-finance). |
| 33 | Phase preset consolidation + agency-marketing plugin (T2 R7) | [prior research/04-plugin-agency-marketing.md](prior%20research/04-plugin-agency-marketing.md) | T2's Round-7 deliverable. **Goal A** (commit `a80daa9`): updates fulfillment's DEFAULT_PHASE_PRESETS to reflect the real plugin lifecycle — Discovery=[website-editor], Design=[website-editor], Development=[website-editor, ecommerce], Onboarding=[+memberships], Live=[+affiliates], Churned=[]. Adds soft-fail in TransitionService.advancePhase + ClientLifecycleService.createWithPhase: when runtime returns "not found"/"not in registry"/"not registered" error, the install is SKIPPED — WARN activity entry + `phase.preset_plugin_skipped` event, phase still advances. AdvancePhaseResult gains `skipped: { pluginId, error }[]`; CreateClientWithPhaseResult.installs[i] gains optional `skipped:true`. Real registry-side errors (auth/scope/dep) still hard-fail. Lifecycle smoke extended with R7 describe-block: catalogue assertion + soft-fail walkthrough (REGISTRY=[website-editor, ecommerce] only; onboarding hop skips memberships, live hop skips memberships+affiliates; client.stage still flips). 11/11 smoke. **Goal B**: `@aqua/plugin-agency-marketing` at `04 the final portal/plugins/agency-marketing/`. `scopePolicy: "agency"`, `core: false`. Domain Campaign (channel email/sms/social/paid/organic/event, state machine draft→scheduled→running→paused/completed→archived, budget+goal+resultActual rollup, ownerStaffId FK), Lead (funnel new→contacted→qualified→converted/unqualified/lost with re-engage paths from un/lost→contacted, append-only contactHistory, secondary indexes by email/campaign/staff), EmailTemplate (3 seeded defaults Welcome/Re-engagement/Newsletter, `{{placeholder}}` substitution helpers). Four services (Campaign/Lead/Template/Report). Six ports. 13 API routes. 5 admin pages. campaignSnapshot reports byChannel + byStatus aggregates; leadFunnel reports conversionRate + per-status counts. tsc-clean; 8/8 smoke. After R7 T2 has shipped 7 plugins (fulfillment + ecommerce + agency-HR + memberships + affiliates + agency-finance + agency-marketing) — full Milesy-internal trio (HR + Finance + Marketing) plus customer-facing trio (shop · join · refer). |
| 35 | End-customer flow (T1 R5) | [prior research/04-end-customer-flow.md](prior%20research/04-end-customer-flow.md) | T1's Round-5 close of the three-level recursion (Agency → Client → End-customer). `users.ts` storage key for end-customers becomes `email|c:<clientId>` so two clients of the same agency may both have a customer named jane@gmail.com; agency/client tier keep the legacy plain-email key. `getUser`/`verifyPassword`/`setUserPassword`/`updateUser` accept optional `UserLookupScope`. New `POST /api/auth/end-customer/signup` (rate-limited, signupsEnabled gate, 409 on duplicate, issues `lk_session_v1` with `clientId + role=end-customer`). `/api/auth/login` accepts an embed-supplied `clientId` and tries the per-client pool first. `Client.endCustomers: ClientEndCustomerConfig` (`signupsEnabled` default true, optional `postLoginReturnUrl`). `/portal/customer/page.tsx` is variant-driven — looks up website-editor install, calls T3's `getActivePortalVariant` for "account" then "login", renders blocks via `<BlockRenderer>`, falls back to welcome card + customer-panel plugin links. New `/portal/customer/[...rest]` catch-all + `resolveCustomerPluginPage` in the resolver. `PanelId += "customer"`; `buildSidebar` filters by `panelId === "customer"` (or `/portal/customer` href) when scope is `customer`. Demo seed adds `demo-shopper@aqua.test`; `/demo/toggle` cycles agency → client → customer → agency; `DemoBanner` shows three POV labels + "Next view → X" button. `resetDemo` already wipes the customer (loop keys by agencyId). Embed `LoginForm` carries `clientId` + `allowSignup` + signup toggle + parent-frame return URL (`?return=` or `client.endCustomers.postLoginReturnUrl` or `${origin}/portal/customer`). Smoke green; tsc + build clean. |
| 34 | Website-editor Round 3 (T3) | [prior research/04-plugin-website-editor-round3.md](prior%20research/04-plugin-website-editor-round3.md) | T3's Round-3 lift. **Goal A**: `pages/CustomisePage.tsx` faithful 898-line port from 02 with five tabs (Branding / Sidebar / Custom-tabs / Login / Export). New libs `lib/customise.ts` (AdminBranding/CustomTab/AdminMode CRUD, drops 02's cross-org brand-plugin resolver), `lib/sidebarLayout.ts` (5-deep panel/folder/link tree with Resource opened to plain string, foundation owns permissions), `lib/loginCustomisation.ts` upgraded from R1 stub to full 02 contract. Q-ASSUMED localStorage persistence pending T1 TenantPort brand-kit setter + PATCH /api/portal/website-editor/customise route. **Goal B**: `RENDERER_REGISTRATIONS: Record<string, BlockComponentType>` cross-plugin renderer map exported from blockRegistry.ts (single source of truth keyed by block id, seeded with 58 native + 8 ecommerce + 3 NEW memberships + 3 NEW affiliates renderers). `getBlockRenderer(type)` resolver + `registerExternalBlockRenderers(plugins)` validation helper that walks each plugin's storefront.blocks and reports missing renderers. BlockRenderer consults RENDERER_REGISTRATIONS first, falls back to BLOCK_REGISTRY. 6 new stub components: 3 membership (paywall/signup/tier-grid) hit `/api/portal/memberships/*`; 3 affiliate (signup/payout-meter/leaderboard) hit `/api/portal/affiliates/*`. **Goal C**: `pages/ThemeDetailPage.tsx` clean rewrite against plugin's per-site lib/theme.ts contract (architectural mismatch with 02's localStorage singleton ThemeConfig made faithful 1063-line lift impractical). Token editor for 13 flat ThemeTokens fields + live preview pane + Save/Set-default/Duplicate/Delete actions + customCss escape hatch. `pages/PagesPage.tsx` re-pointed at EditorPage list via lib/editorPages.ts (customPages backend deferred to R4 — likely separate page-builder plugin). Smoke 40/40 (+9 cross-plugin renderer tests). tsc clean. R4 deferred: PageDetailPage / SitesPage (3264 LOC) / customise server-side persistence. |
| 36 | Client-CRM plugin (T2 R8) | [prior research/04-plugin-client-crm.md](prior%20research/04-plugin-client-crm.md) | T2's Round-8 deliverable. `@aqua/plugin-client-crm` at `04 the final portal/plugins/client-crm/`. `scopePolicy: "client"`, `core: false`, `requires: []` (no hard deps; soft-integrates with memberships + ecommerce + affiliates via OPTIONAL injected ports — return-null-when-absent pattern from R5). Pairs with T1 R5: end-customer signups auto-appear as Contacts via `mergeFromUser`. Domain Contact (per-(agencyId,clientId) email uniqueness, optional endCustomerUserId link, status active/unsubscribed/bounced/deleted, tags + freeform attributes, secondary indexes by email/userId, firstSeenAt + lastSeenAt with engagement-driven bumps), Segment (4 seeded defaults All/New/Engaged/Dormant with `{{now-Nd}}` sliding-window placeholder resolution, AND-of-conditions evaluator across 7 field types incl. cross-plugin membershipPlanId, isDefault non-deletable), ActivityRecord (10 kinds incl. signup/order/subscription/affiliate_referral/note, append-only timeline, idempotent cross-plugin ingest on synthesized source-id). Three services (Contact/Segment/Activity). Six standard ports + two OPTIONAL (MembershipBenefits / EcommerceOrders) — graceful null when absent. 14 API routes (admin + customer + `/events/ingest` for foundation event router). 6 admin/customer pages. 1 storefront block id (`crm-contact-form` — T3 R3 will register renderer). Bulk import capped at 1000 rows; existing-email rows patch (tags merged + deduped, attributes merged). `mergeFromUser` reconciles a manual contact with a foundation User by email — same id returned, no duplicate created. `backfillFromEcommerce` pulls recent orders via the optional port and ingests as activity. tsc-clean; 10/10 smoke pass. Foundation pending: workspace dep + transpilePackages + side-effect-import + `_registry.ts` append + `ActivityCategory` += "crm" + UserPort projection (now used across 5 of T2's plugins) + MembershipBenefitsPort + EcommerceOrdersPort wiring + cross-plugin event router fan-out to `/events/ingest`. After R8 T2 has shipped 8 plugins (fulfillment + ecommerce + agency-HR + memberships + affiliates + agency-finance + agency-marketing + client-crm); 74 smoke cases catalogue-wide. |
| 37 | Website-editor Round 4 (T3) | [prior research/04-plugin-website-editor-round4.md](prior%20research/04-plugin-website-editor-round4.md) | T3's Round-4 close of the admin-page parity-with-02. **Goal A**: `pages/SitesPage.tsx` faithful 3,264-line port — list + per-site settings (logo / favicon / domains / primaryDomain / customHead / customBody / smoothScroll / customCursor / siteNavigationJsonLd / enabledProductRanges / socialHandles) + theme picker + portal-variant overrides + embed config + GitHub repo URL. New libs: `lib/sitesAdmin.ts` (faithful port of 02's synchronous localStorage CRUD, sits alongside R2's API-backed `lib/sites.ts` until foundation server-side wiring lands; `setActivityLogger` pluggable so foundation can wire its activity log), `lib/portalSettings.ts` (cloud-architected GitHub + database + deployment sections; falls back to defaults when foundation route absent), `lib/themeVariants.ts` rebuilt with 5 built-in variants (Default/Light/Dark/Sand/Midnight) + custom-variant CRUD + per-site default cursor (Round-1 `light`/`dark`/`system` selector kept as `ThemeAppearance`), `lib/portalEditMode.ts` (`buildEditorUrl` + edit-mode flag), `lib/domains.ts` (Vercel attach/detach/verify shim — Q-ASSUMED until T1 wires `/api/portal/website-editor/domains/*` proxy). **Goal B**: `lib/customPages.ts` faithful port of 02's localStorage block system — distinct from EditorPage, 9 typed block kinds (hero/richText/image/gallery/quote/embed/divider/cta/html), CustomPage CRUD + onCustomPagesChange, R1 compat shims preserved. **Goal C**: `pages/PageDetailPage.tsx` faithful port consuming `lib/customPages.ts` — title/slug/nav inputs + per-block editors + SEO panel (title/description/OG/canonical/robots/JSON-LD) + publish/duplicate/delete + sticky footer; `useParams().id` OR `?pageId=...` accepted. `components/RichEditor.tsx` textarea fallback. After R4 the website-editor admin surface is parity-with-02 (every admin page from 02 has a faithful or clean-rewrite port). tsc clean; smoke 40/40 unchanged. R5 deferred: server-side persistence for sitesAdmin/customPages/customise/loginCustomisation; Vercel domain proxy; portal-settings persistence; real RichEditor host. |
| 39 | Foundation Round 6 (T1) | [prior research/04-foundation-round6.md](prior%20research/04-foundation-round6.md) | T1's Round-6 mass plugin wire-up + cross-plugin event router. After R5 the foundation hosted 3 live plugins while T2 had 6 more on disk uncovered. R6 mounts every shipped plugin: 6 file:.. workspace deps + transpilePackages, 6 new `foundation-adapters/<plugin>Foundation.ts` side-effect-imports, `_registry.ts` append for all 6 manifests. Shared `_foundationPorts.ts` centralises tenant/activity/events/pluginInstalls/user; `_crossPluginPorts.ts` brokers the cross-plugin reads (ecommerceOrders → affiliates + client-crm with two projection shapes; membershipBenefits → client-crm). `ActivityCategory` union widens with hr/memberships/affiliates/finance/marketing/crm; each `register*Foundation` call uses an `as unknown as Parameters<...>` cast to bridge plugin-vendored ActivityCategory drift. **Cross-plugin event router** — `eventBus.subscribeForPlugin(pluginId, eventName, handler)` lands; emit() does two-stage fan-out (direct + tenant-filtered for plugin subscribers). `_eventSubscribers.ts` wires affiliates ← `order.created`, client-crm ← `order.created` / `affiliate.attribution_recorded` / `membership.subscription_started|canceled`. `_validate.ts` widens VALID_CATEGORIES with growth/hr/finance + VALID_PANEL_IDS with the per-plugin panels in flight. `sidebarLayout.ts` surfaces "discovered" panels (panelIds not in DEFAULT_PANELS) between Tools/Settings with auto-derived labels — future plugins ship new panel ids without a foundation patch. `membershipsFoundation.stripeFor` returns a NOOP StripePort so memberships's containerFor builds in dev (paid flows throw clearly; real Stripe SDK adapter is foundation-pending). `seedDemoAgency` extended to install 5 client-side (website-editor → ecommerce → memberships → affiliates → client-crm) + 3 agency-side (agency-hr / agency-finance / agency-marketing) for a 9-plugin demo. `scripts/smoke.mjs` 35/35 pass: /demo cold + 9 install entries + 11 nav URLs 200 + 6 API surfaces 200 + full POV cycle. tsc + build clean. |
| 40 | Website-editor Round 5 (T3) | [prior research/04-plugin-website-editor-round5.md](prior%20research/04-plugin-website-editor-round5.md) | T3's Round-5 fills in real React components for the cross-plugin storefront blocks registered as stubs in R3. **Phase A** (ecommerce 8 + donation): `ecommerceBridge.tsx` upgraded — `useCart()` now reads/writes lk_cart_v1 localStorage with cross-tab sync, real `ProductVariantPicker` swatch UI, `goToStripeCheckout()` adapter posts cart to `/api/portal/ecommerce/stripe/checkout`, plus `fetchOrderBySessionId` / `fetchProductVariants` / `searchProducts` helpers. OrderSuccessBlock fetches by `?session_id=` + renders line items + total; PaymentButtonBlock invokes Stripe checkout with busy/error states; DonationButtonBlock re-pointed at ecommerce stripe/checkout (was Round-1 monolith path); ProductSearchBlock re-pointed at /api/portal/ecommerce/products with credentials. **Phase B** (memberships 3 + affiliates 3 + forms 1 + crm 1): MembershipPaywallBlock consults `/memberships/me` for active subscription matching `props.requirePlanIds`; MembershipSignupBlock corrected body shape `{planId, billing}` to match memberships R4 server, 401/404/free-tier flows handled; AffiliateSignupBlock corrected body shape `{payoutEmail, displayName}`; AffiliatePayoutMeterBlock rolls up attributions + payouts from `/affiliates/me`; AffiliateLeaderboardBlock degrades to empty state on 404 (Q-ASSUMED endpoint pending T2 R10). NEW: `FormRenderBlock` (renders 11 field kinds from `/forms/public/form/:id`, submits with thank-you/redirect handling) + `CrmContactFormBlock` (delegates to FormRenderBlock when formId set, else built-in contact form posting to Q-ASSUMED `/client-crm/public/contact`). `RENDERER_REGISTRATIONS` grew to 66 entries (58 native + 3 memberships + 3 affiliates + 1 form-render + 1 crm-contact-form; 8 ecommerce already in native via R2 Phase A). **Phase C**: new smoke `cross-plugin-renderers.test.ts` (25 tests covering registry resolution + registerExternalBlockRenderers happy/error paths + count sanity); blocks.test.ts grew 40→42 (added 2 cross-plugin ids). Total **67/67 pass**. tsc clean. R6 deferred: server-side cart, Stripe Subscription mode for recurring donations, real RichEditor host, storefront SSR. T2 R10 cross-team: `/affiliates/leaderboard`, `/client-crm/public/contact`, optional `/orders/by-session/:id` confirmation. |
| 38 | Forms plugin (T2 R9) | [prior research/04-plugin-forms.md](prior%20research/04-plugin-forms.md) | T2's Round-9 deliverable. `@aqua/plugin-forms` at `04 the final portal/plugins/forms/`. `scopePolicy: "either"` (works at agency or client scope), `core: false`, no hard deps. Soft-integrates with client-CRM/affiliates/memberships via cross-plugin event payloads + admin-configurable webhook URLs — zero source coupling between plugins. Domain FormDefinition (11 field kinds: text/email/phone/textarea/select/multiselect/radio/checkbox/number/date/hidden, state machine draft→published→archived, per-form submissionCount), FormField (validation rules: required/minLength/maxLength/pattern/min/max + per-kind email/number/select/multiselect checks, optional `attributeKey` hint for CRM ingestion), SubmitAction (4 kinds: store-only/redirect/thank-you/external-webhook with optional notifyEmails), Submission (idempotent on fnv1a hash of formId+identifier+sortedValues — same values collapse onto prior row, submissionCount NOT bumped on collapsed re-submit), FormTemplate (3 seeded defaults Contact/Newsletter Signup/Lead Capture with `{ kind: "thank-you" }` action). Four services (Form/Submission/Notification/Template). Six standard ports + one OPTIONAL EmailQueuePort (agency-marketing brokers when installed; absent → events still fire, no email queued). 13 API routes — including 2 PUBLIC routes (`POST public/submit/:formId` + `GET public/form/:formId`) flagged `public: true` for the catch-all dispatcher to skip session check. 5 admin pages incl. structured FormBuilderPage (no drag-drop in v1 — table of fields with inline edit; polish round defers drag-drop). 1 storefront block id (`form-render` — T3 owns rendering). Stable cross-plugin event payloads documented (`forms.submission.created`, `forms.submission.validation_failed`, `forms.submission.status_changed`, `forms.notification.requested`). tsc-clean; 8/8 smoke pass. Foundation pending: workspace dep + transpilePackages + side-effect-import + `_registry.ts` append + `ActivityCategory` += "forms" + catch-all `public: true` honouring (shared item with memberships R4 webhook) + cross-plugin event router fan-out for forms.submission.created → CRM ingest + webhook POST + email enqueue + EmailQueuePort wiring (no-op stub until agency-marketing send-time integration ships). After R9 T2 has shipped 9 plugins (fulfillment + ecommerce + agency-HR + memberships + affiliates + agency-finance + agency-marketing + client-crm + forms); 82 smoke cases catalogue-wide. |
| 41 | Email-sender plugin (T2 R10) | [prior research/04-plugin-email-sender.md](prior%20research/04-plugin-email-sender.md) | T2's Round-10 deliverable. `@aqua/plugin-email-sender` at `04 the final portal/plugins/email-sender/`. `scopePolicy: "agency"`, `core: false`, no hard deps. Single point of egress for every transactional / notification email across the agency portal. Domain EmailMessage (state machine queued→sending→sent/failed/bounced, idempotency key `${triggeredByPlugin}:${externalRef}` else `${plugin}:${sortedTo}:${fnv1a(body)}` — collapses event-bus retries), SenderIdentity (per-agency, isDefault flag, status active/pending/failed, verifyDomain stub), ProviderConfig (provider postmark/sendgrid/resend/smtp/none, masked apiKeyMasked + full key kept at `provider/api-key`, webhookSecret, status active/unconfigured/error). Four services: EmailService (enqueue + state transitions + 4 cross-plugin subscribers), DeliveryService (queued→sending→sent/failed via active driver, retry path via resetForRetry), WebhookService (verify-by-driver + dedupe by `${RecordType}:${MessageID}` + status update + emit), IdentityService, ProviderService. Driver pattern: PostmarkDriver (live; injectable fetchImpl, query-param `?secret=` exact-match webhook verify), NoopDriver (live; synthetic `noop_<id>` ref), StubDriver (sendgrid/resend/smtp throw "R11 stub"). Five standard ports + one OPTIONAL MarketingTemplatePort (agency-marketing's EmailTemplate store + optional render fn — absent → templateless enqueue still works, templateId throws cleanly). 12 API routes — including 1 PUBLIC route (`POST public/webhook/postmark`, `public:true`). 3 admin pages (Outbox / Settings / Logs). No storefront blocks (server-side only). Cross-plugin event subscribers declared via `EVENT_SUBSCRIPTIONS` const array on the foundation adapter — foundation R6 router reads at boot + subscribes 4 handlers on the live EmailService: forms.notification.requested → `onFormsNotificationRequested`, membership.subscription_changed → `onMembershipSubscriptionChanged` (welcome/cancellation), affiliate.payout_completed → `onAffiliatePayoutCompleted`, auth.bootstrap.signup → `onAuthBootstrapSignup`. `onInstall` bootstraps default sender identity from settings (defaultFromName/defaultFromEmail). tsc-clean; 7/7 smoke pass. Foundation pending: workspace dep + transpilePackages + side-effect-import + `_registry.ts` append + `ActivityCategory` += "email" + cross-plugin event router subscriber wiring (item now load-bearing across forms/memberships/affiliates/auth) + MarketingTemplatePort projection from agency-marketing + catch-all `public: true` honouring for the Postmark webhook. After R10 T2 has shipped 10 plugins (… + email-sender); 89 smoke cases catalogue-wide. |
| 42 | Foundation Round 7 — Postgres backend (T1) | [prior research/04-foundation-round7-postgres.md](prior%20research/04-foundation-round7-postgres.md) | T1's Round-7 swaps the file backend → Postgres for production (architecture §13's parked v1 item). Single `portal_kv (key TEXT PK, value JSONB, updated_at TIMESTAMPTZ)` table + btree on `key text_pattern_ops` for prefix scans. New `src/server/storagePostgres.ts` driver — lazy `pg.Pool` from `DATABASE_URL`, TLS auto-detect (Neon/Supabase/Vercel-Postgres), `loadBlob`/`saveBlob` against single-row blob keyed `__portal_state__` (Q-ASSUMED — keeps the existing snapshot+debounced-flush model intact, no consumer refactor). `storage.ts` slots the new backend next to file/memory/kv with dynamic import so `pg` stays out of the parse-time path when PORTAL_BACKEND=file (the default). Implicit promotion: `DATABASE_URL` set + `PORTAL_BACKEND` unset → postgres takes over. New `scripts/schema.sql`, `scripts/migrate-file-to-postgres.mjs` (idempotent ON CONFLICT upsert, DRY_RUN=1 supported), `scripts/smoke-postgres.mjs` (8/8 pass — schema + index + round-trip + idempotency + prefix scan + payload size + cleanup). RLS deferred to R8 — single-row blob layout would gate the row not the in-blob fields; existing `withTenantScope` at the foundation layer is the operating defense. `npm run smoke:postgres` + `npm run migrate:file-to-postgres` aliases. .env.example documents DATABASE_URL + pool tunables. tsc clean. Production runbook: createdb → schema.sql → migrate → set DATABASE_URL → restart. |
| 43 | Client portal — Luv & Ker / Felicia (T5 R1) | [prior research/04-client-portal-luv-and-ker.md](prior%20research/04-client-portal-luv-and-ker.md) | T5's Round-1 deliverable. First real per-client portal under `04 the final portal/clients/luv-and-ker/` — the canonical reference shape T2 R11's "Export to repo" generator must reverse-engineer. Self-contained Next.js 16 + React 19 + Tailwind 4 app, dev port 4040. `package.json` declares 6 plugin workspace deps (website-editor / ecommerce / memberships / affiliates / client-crm / forms — agency-side plugins deliberately excluded); `next.config.ts` `transpilePackages` matches; `.npmrc install-links=true` for Turbopack. `portal-config.json` is the manifest the generator emits — 5 top-level objects (client / brand / auth / installedPlugins / portalVariants / content) sourced from the shared portal at "Export to repo" time. Brand kit values lifted from `portal/src/lib/server/demoSeed.ts` Felicia mirror (#F97316 orange / #FFF7ED cream / #7C3AED accent / Playfair Display + 8px radius). `src/app/layout.tsx` reads portal-config + injects `<style>:root{--brand-*}</style>` brand tokens (mirror of `portal/src/lib/chrome/brandKit.ts`). Pages: `/` storefront landing (Hero / FeaturedProducts / BrandStory / Newsletter), `/login` + `LoginPanel.tsx`, `/embed/login` iframe-able branded, gated `/account` + `/orders` + `/affiliates` (server-side `getSessionUser` redirect to `/login`), `/shop` + `[id]`, `/cart`, `/checkout`, `/order-success`. `src/app/api/[...path]/route.ts` is a catch-all proxy forwarding every method to PORTAL_API_ORIGIN (default localhost:3030 in dev / milesymedia.com in prod) — hop-by-hop headers stripped, request + response bodies streamed, Set-Cookie preserved, 502 fallback when upstream unreachable. `src/server/pluginDispatch.ts` lazily caches each plugin's manifest (loose typing — block descriptors use `type` not `id`). `src/lib/sessionUser.ts` forwards `lk_session_v1` to `/api/auth/me` for member-page identification. `npm install` + `npx tsc --noEmit` clean; smoke flow green: `/`, `/login`, `/embed/login`, `/shop` all 200, `/account` 307→/login, `/api/auth/me` proxies upstream's 401 correctly. Hand-coded variants ship for the four PortalRoles until the website-editor publishes block trees (slot-in swap, no content migration). Cross-team handoffs: T2 R11 generator must produce this exact tree shape; T6 R1 attaches `luvandker.com` + sets PORTAL_API_ORIGIN env; T1 cookie-domain config (Q-ASSUMED keeping proxy-passthrough for v1). |
| 45 | UX + a11y audit (T4 R1 Phase A) | [prior research/04-ux-audit.md](prior%20research/04-ux-audit.md) | T4's Round-1 Phase A inspection of the entire `04` surface (chrome / 7 foundation pages / 70 plugin admin pages / 58+8+6 storefront blocks). Headline numbers: 0 focus-visible utilities, 0 ErrorBoundaries, 0 animate-pulse skeletons in scope, 29 native `confirm()` calls, 28 `window.location.reload()` patterns, 7/10 modals declare `role="dialog"` but none traps focus, sidebar fixed `w-60` with no mobile collapse, no skip-to-content link, no toast/live-region. Per-plugin punch list across all 9 plugins (each scored loading/empty/error/focus/aria/kbd/mobile). Plugin admin uses prefixed CSS class names (`affiliates-*`, `ecom-*`, etc.) but no actual CSS rules ever shipped — pages render largely unstyled. Prioritised plan groups fixes into P0 (ships R1, everyone benefits — global focus ring, skip link, skeleton/empty/error primitives, mobile sidebar, focus trap, toast hook, ConfirmDialog, contrast validator), P1 (ships if time), P2 (round 2). |
| 46 | UX + accessibility pass (T4 R1) | [prior research/04-ux-accessibility-pass.md](prior%20research/04-ux-accessibility-pass.md) | T4's Round-1 implementation closing the punch list from chapter 45. Shared UI primitives at `portal/src/components/ui/` (LoadingSkeleton + InlineSkeleton, EmptyState, ErrorBoundary, SkipToContent, ConfirmDialog) + a11y hooks at `portal/src/lib/a11y/` (useFocusTrap, useArrowNav, useViewport, contrastValidator). `globals.css` adds: global `:focus-visible` 2px brand-accent ring on every interactive element, `sr-only` + `focus-visible:not-sr-only` for SkipToContent, `aqua-pulse` keyframes, `prefers-reduced-motion` guard, plus a 270-line "Plugin admin baseline" block with attribute-suffix selectors that style every plugin's prefixed class vocabulary at once (status pills colour-code by suffix, modals auto-fullscreen on `<640px`, tap targets bumped to 44px on `pointer: coarse`). Chrome upgrades: Sidebar gets `aria-label="Primary"`/`aria-current="page"`/`<section aria-labelledby>` per panel + hides at `<md`; new `MobileNav.tsx` slide-over drawer with focus-trap + Escape close + 44px hamburger; Topbar wraps gracefully + truncates + emits `aria-label` on icon-only links; `ThemeInjector` wires the contrast validator (dev-mode console.warn on AA failures). Layouts: `RootLayout` mounts `<SkipToContent>`; agency/client/customer layouts wrap `<main id="main-content">` with `<ErrorBoundary>` + pass `panels`/`tenantLabel`/`currentPath` to Topbar so the mobile drawer mirrors desktop. The 3 plugin-page catch-all resolvers wrap rendered components in `<ErrorBoundary label="<pluginId>/<page.path>">`. Six high-traffic plugin list components (FormsList / AffiliatesList / ProductsList / CampaignsPage / InvoicesList / ExpensesList) get inline `role="status"` empty-state cards. New `scripts/smoke-ux.mjs` + `npm run smoke:ux` hits 8 representative pages × 3 viewports (375/768/1280) asserting status, skip-link, main-content landmark, primary-nav aria, no app-error markers. tsc clean. Mesh hazard logged: T5/T6's `git pull --rebase --autostash` swept some Phase B step 2 work into commit `a943673` — content shipped, attribution mislabelled. R2 deferred: ConfirmDialog adoption across 29 `confirm()` sites, useArrowNav adoption, field-level form errors, toast system, brand-orange→brand-accent rewrite, drag-drop a11y, light-mode block variants, real Playwright visual regression. |
| 47 | Website-editor Round 6 (T3) | [prior research/04-plugin-website-editor-round6.md](prior%20research/04-plugin-website-editor-round6.md) | T3's Round-6 wires the editor Save → `clients/<slug>/` per-client repo (per architecture extension chapter 19b). Four goals shipped against ports T2 R11/T6 haven't yet provided — graceful degradation hides UI when ports absent so dev mode still works. **Goal A**: `SaveTargetToggle` topbar widget + `lib/saveTarget.ts` (per-client localStorage cursor + default-per-phase resolver: phase=live AND clients/<slug>/ exists AND port available → "client-repo"; otherwise "shared-portal"). **Goal B**: `lib/savePipeline.ts` is the only file the save UI talks to — `savePage / publishPage / saveTheme / saveCustomPage / setActivePortalVariant / previewChanges` branch on the active save target. shared-portal → existing R2 storage path; client-repo → `PortalExportPort.savePage(...)` etc. When the port returns `fallbackToFullReexport: true`, pipeline auto-runs `materialize()` and tags the result so `SaveResultBanner` surfaces "(full re-export)" hint. When the port isn't injected, client-repo saves return `ok:false + target:"shared-portal"` (toggle should already have hidden itself). `server/extensionPorts.ts` declares `PortalExportPort` (slugForClient + clientRepoExists + savePage/saveCustomPage/saveTheme/setActivePortalVariant + previewChanges + materialize) and `GitOpsPort` (status/stage/unstage/commit/push/openPullRequest) with `setPortalExportPort` / `setGitOpsPort` injection helpers. **Goal C**: `DiffPreviewPane` renders the file-list returned by `previewChanges` (A/M/D badges + monospace path + byte-count) above the canvas in client-repo mode; `SaveResultBanner` auto-dismisses shared-portal saves at 2s, persists for client-repo saves with file-count + "Open commit →" deep-link. **Goal D**: new admin page `pages/GitStatusPage.tsx` (panelId `growth`, route `/portal/clients/[clientId]/git-status`) — client picker + branch + ahead/behind + staged/unstaged file lists with per-row + bulk Stage/Unstage + Commit/Push/Open PR buttons. Renders an inline "git ops port not wired" notice when T6 hasn't shipped GitOpsPort. `lib/gitOps.ts` wraps `/api/portal/website-editor/git/*` with 404→`{available:false}` graceful degradation. New smoke `save-target.test.ts` (25 assertions: default-per-phase × 4 + routing × 4 + fallback × 3 + missing-port × 2 + previewChanges × 4 + per-result `target` checks). Total smoke **92/92 pass** (42 blocks + 25 cross-plugin renderers + 25 save-target). Manifest navItems 8→9, pages 11→12. tsc clean. R7+ deferred: real-time collab (architecture §13 parked), domain-attach UI (T6's territory), auto-stage saved files toggle, SSR/static export. Cross-team handoffs: T1 brokers the new optional ports + mounts `/api/portal/website-editor/git/*` HTTP proxy; T2 R11 implements PortalExportPort (initial round can ship `materialize` only with `fallbackToFullReexport:true` from incremental saves — pipeline auto-detects); T2 R12 polishes incremental save APIs; T6 R1 implements GitOpsPort wrapping `git` CLI. |
| 48 | milesymedia ↔ portal stitch (T1 R8) | [prior research/04-milesymedia-portal-stitch.md](prior%20research/04-milesymedia-portal-stitch.md) | T1's Round-8 stitches milesymedia static site + Aqua portal as one origin in dev (`localhost:3030`) + production (Vercel single project). Files stay separate in repo (`milesymedia website/` + `portal/`) — Ed's "puzzle piece" requirement. New `portal/scripts/prepare-milesy.mjs` copies the static site → `portal/public/_milesy/` (idempotent; runs as `predev` and `prebuild` so dev + Vercel build use one canonical copy step). `next.config.ts` `rewrites().beforeFiles` mirrors the production `vercel.json` rewrites: `/`, `/index.html`, `/login.html`, `/admin.html`, `/styles.css` → `/_milesy/<file>`. `npm run dev:all` is an alias for `npm run dev` (single port, single server — Q-ASSUMED over the prompt's two-server `concurrently` pattern: simpler, no extra script to maintain, no config drift between dev and prod). T6 R1 Phase A (commit `359b476`) already shipped the production-side root `vercel.json` + `build-portal.mjs`; R8 mirrors that surface in dev. Static site `data-portal-base` meta defaults to `""` (same-origin) since R8 across all 3 pages (index/login/admin); `?portalBase=…` query param overrides for standalone-preview workflows. R4 demo + R5 end-customer chapters appended with same-origin notes. `public/_milesy/` added to portal `.gitignore` (build artefact). Smoke green: `/` → milesymedia landing 200, `/styles.css` → 200 text/css, `/login` → Next.js login, `/login.html` → static login mock, `/admin.html` → static admin mock, `/demo` → 307 + isDemo cookie. tsc clean. |
| 49 | Portal-export plugin (T2 R11) | [prior research/04-plugin-portal-export.md](prior%20research/04-plugin-portal-export.md) | T2's Round-11 deliverable. `@aqua/plugin-portal-export` at `04 the final portal/plugins/portal-export/`. `scopePolicy: "either"`, `core: false`, no hard deps (soft-reads website-editor via optional `WebsiteEditorReaderPort`). Generator that materializes a Live client's content into `clients/<slug>/` as a self-contained Next.js app. Mirrors T5's `clients/luv-and-ker/` shape exactly — package.json with file:.. plugin workspace deps (union of installed + preset, sorted), next.config.ts with security headers + CSP, tsconfig.json + postcss.config.mjs + tailwind.config.ts (brand colors injected), src/app/{layout, page, globals.css}, src/lib/{brandKit, portalConfig}, portal-config.json (with `_generatedFingerprints` ledger baked in for idempotent re-export). **Idempotent re-export** via fingerprint ledger: on re-run, computeDiff classifies each planned file as added (write) / changed (we owned, overwrite) / preserved (operator hand-edited, KEEP) / unchanged. Operator hand-edits to materialized files are preserved byte-for-byte — first-time runs treat all existing files as preserved (no ledger means no claim of authorship). Domain: PortalPreset, CollectedClientState, MaterializedFile, ExportPlan/Diff/Record, PortalConfigDoc, GeneratedFingerprintMap. Two services (PresetService + ExportService — collect → plan → diff → export(write); HistoryService folded into ExportService.listHistory/getHistory). Five standard ports + 2 OPTIONAL: `WebsiteEditorReaderPort` (active variants + custom content + theme tokens), `FilesystemPort` (foundation supplies real fs/promises in dev; smoke injects in-memory Map). 8 API routes (presets list/get + state preview + plan + export + history + pr/open stub). 3 admin pages (Export / Presets / History). 4 v1 preset starters bundled JSON: `skincare-brand` (Felicia's pattern: editor + ecommerce + memberships + affiliates + client-crm + forms), `service-portal` (editor + fulfillment + forms + client-crm), `membership-only` (editor + memberships + client-crm), `affiliate-only` (editor + affiliates + client-crm). Brand-kit override on plan(): client brand wins over preset default; explicit brandOverride beats both. Editor-supplied portal variants override preset where they overlap; preset fills missing roles. tsc-clean; 5/5 smoke pass via `npx tsx --test src/__smoke__/export.test.ts` (materialize fixture + idempotent no-op + idempotent preserve + 4 presets validate + brand override beats preset). Foundation pending: workspace dep + transpilePackages + side-effect-import + `_registry.ts` append + `ActivityCategory` += "export" + FilesystemPort wiring (real `fs/promises`, root at `04 the final portal/clients/`) + WebsiteEditorReaderPort projection from website-editor + GitHub PR-open integration (replaces `pr/open` stub; out of scope for v1). After R11 T2 has shipped 11 plugins (… + portal-export); 94 smoke cases catalogue-wide. |
| 44 | Deployment + custom domains + observability (T6 R1) | [prior research/04-deployment-domains-observability.md](prior%20research/04-deployment-domains-observability.md) | T6's Round-1 deliverable. Wires the production stack. **Phase A** (commit `359b476`): single Vercel project deploys the Next.js portal at `04 the final portal/portal/`; `scripts/build-portal.mjs` copies `04 the final portal/milesymedia website/*` into `portal/public/_milesy/` before `next build` so milesymedia static + Aqua portal share one origin (cookies + auth work end-to-end). Repo-root `vercel.json` rewrites `/`, `/index.html`, `/login.html`, `/admin.html`, `/styles.css` → `/_milesy/<file>`. `scripts/deploy-vercel.mjs --target=portal\|clients/<slug> [--prod]` orchestrates per-target Vercel CLI deploys (assumes operator has `vercel login`). `.vercelignore` updated; `scripts/templates/{client-vercel.json,client.env.example,README.md}` are the per-Live-client deploy templates T2 R11's Export-to-repo generator drops into each new client folder. **Why per-client = separate Vercel projects**: production isolation, per-domain Vercel API surface, branded build artefact, fork-friendly ejection — see chapter §1. **Phase B** (commit `ef2e82f`): env taxonomy. `portal/.env.example` extended with VERCEL_TOKEN/VERCEL_TEAM_ID + SENTRY_DSN/NEXT_PUBLIC_SENTRY_DSN/SENTRY_ENVIRONMENT/SENTRY_TRACES_SAMPLE_RATE; per-client template adds NEXT_PUBLIC_AGENCY_ID/CLIENT_ID/PORTAL_SLUG identity. **Per-deployment env vs per-install plugin config split** load-bearing: provider creds (Stripe / Postmark / etc) NEVER in env — pool-model multi-tenancy means one deploy serves hundreds of agencies + thousands of clients; per-tenant env doesn't scale. Per-install config is rotatable without redeploys. Naming Q-ASSUMED honoured T1's existing `PORTAL_SESSION_SECRET` / `PORTAL_BACKEND` / `NEXT_PUBLIC_PORTAL_BASE_URL` over the prompt's `LK_*` / `STORAGE_BACKEND` / `_BASE` labels. **Phase C** (commit `a943673` — landed under T5's mislabel due to parallel-staging in the mesh): `@aqua/plugin-domains` lifted from `02 felicias aqua portal work/src/lib/vercel/server.ts`. Self-contained at `04 the final portal/plugins/domains/`; `scopePolicy: "either"`, `core: false`. Domain model `DomainRecord { id, agencyId, clientId?, hostname, vercelProjectId, vercelTeamId?, status: pending\|verified\|error, pending: DnsRequirement[], lastError?, createdAt/updatedAt/lastCheckedAt?, attachedBy? }`. Storage `domains/<id>` + `by-host/<hostname>` index. 5 API routes (status / list / attach / verify / remove). VERCEL_TOKEN from env (operator-level secret); `vercelProjectId` per-record so one install manages many per-client projects. Without VERCEL_TOKEN: captures hostname locally, returns `configured:false` + `error:"vercel-token-not-configured"`, manual-DNS runbook still works (chapter §5). 8/8 smoke pass (isConfigured / attach captures + skips API / idempotent on duplicate / list scopes by tenant / verify not-found / remove drops record / rejects empty hostname / rejects missing projectId). tsc-clean standalone. **Phase D** (commit `6045568`): `04 the final portal/portal/src/lib/server/observability.ts` server-side wrapper. Sentry env-gated via dynamic `import("@sentry/nextjs")` — optional-dep contract holds (no error if SDK isn't installed; `console.error` always fires). Public API: captureError / recordBreadcrumb / withApiObservability(handler, {route, resolveBreadcrumb}) / setSessionScope / flushObservability / isObservabilityConfigured. Per-tenant breadcrumb shape `{agencyId?, clientId?, userId?, pluginId?, extra?}` becomes Sentry tags + user — cross-Sentry-project correlation across shared portal + each per-client portal works via these tags. Vercel Analytics is the lighter complement, opt-in via dashboard toggle (zero config). OpenTelemetry deferred to R2. **Foundation pending** (across phases): workspace dep + transpilePackages + side-effect-import `domainsFoundation.ts` + `_registry.ts` append + `ActivityCategory` += "domains" for the domains plugin; for observability, optionally lift the wrapper into a `@aqua/observability` workspace pkg once the plugin contract unifies. Production turn-on: install `@sentry/nextjs` + set DSN env + redeploy — no code rewire. **Custom domain runbook** documented in chapter §5 (one-time token setup + per-client attach loop). |
| 51 | UX storefront + per-client + perf pass (T4 R2) | [prior research/04-ux-storefront-perf-pass.md](prior%20research/04-ux-storefront-perf-pass.md) | T4's Round-2 polish on the surfaces R1 didn't reach. **Phase A** (commit `27c78ed`): brand-orange→brand-accent sweep across 60 occurrences in 31 storefront blocks (the audit-flagged P1 item — `var(--brand-orange, #ff6b35)` → `var(--brand-accent, #ff6b35)` so client brand kits paint the storefront). 6 highest-impact cross-plugin renderers (AffiliateLeaderboard / AffiliatePayoutMeter / AffiliateSignup / MembershipSignup / FormRender / CrmContactForm + ProductGrid) gain proper loading/error/empty/aria/touch — skeleton-pulse via inline `aqua-pulse` keyframes, `role="alert"` + Retry buttons, `aria-label` on each section/form, `min-height: 44` on every interactive, `aria-pressed` on toggles, sr-only labels with `autoComplete` hints. 401/404 routed to friendly empty states (e.g. "Sign in to see your earnings"); other failures → error+retry. **Phase B** (commit `a223692`): `lib/a11y/isEmbedded.ts` (`isEmbeddedNow()` + `useIsEmbedded()` hook, SSR-safe, gracefully handles cross-origin frame access throw). `embed/login` Suspense fallback upgraded from empty `<div>` to 3-bar skeleton; brand logo gets explicit `height={40}` + descriptive alt; `<main id="main-content" data-embed="true">`. `MobileNav` switches from `fixed inset-0` to `absolute inset-0` when embedded, so the drawer stays inside the iframe instead of escaping to overlay the parent frame. `/portal/customer` FallbackCard gains `role="status"`. **Phase C** (commit `9023d95`): `clients/luv-and-ker/` adopts the foundation primitive set as in-tree copies (per-client portals are independent Next.js apps so each carries its own — T2 R11's generator should drop this same shape into every new client). New: `components/ui/{SkipToContent,ErrorBoundary,EmptyState}.tsx` + `lib/a11y/contrastValidator.ts`. `globals.css` gains the polish layer: global `:focus-visible` 2px brand-accent ring, sr-only utilities, aqua-pulse keyframes, `prefers-reduced-motion` guard (kills pulse + smooth-scroll), `pointer: coarse` 44px tap targets on `.btn-primary` / `.btn-ghost` / buttons. Layout mounts SkipToContent + dev-mode `validatePalette()` console.warn (Felicia palette `#F97316/#FFF7ED/#7C3AED/#1A1A1A` passes AA — no warnings expected). All 9 page-level `<main>` get `id="main-content"`; storefront landing wraps in `<ErrorBoundary label="storefront">`. **Phase D** (commit `da37a93`): `scripts/smoke-perf.mjs` + `npm run smoke:perf` lightweight fetch-based check — per representative page asserts response time ≤ 2500ms (local-dev FCP-equivalent target since SSR HTML is the dominant blocking resource), HTML payload ≤ per-page KB budget (landing 80 / login 60 / portal home 200 / portal list 100 / plugin pages 200), status 2xx/3xx, cumulative totals printed. Real Lighthouse with TTI/CLS deferred to R3 (needs Puppeteer + Chromium install — out of scope for polish round; SSR-render time + payload size capture 80% of the FCP/LCP signal). Tunable via AQUA_BUDGET_MS / AQUA_BUDGET_KB / AQUA_BASE env. tsc clean across all 4 commits (foundation portal + per-client portal both verified). R3 deferred: `<BrandImage>` next/image wrapper with per-deploy allowlist (single `<img>` in foundation portal already height-attr fixed; storefront blocks already use `loading="lazy"`), real Puppeteer Lighthouse smoke, `@next/bundle-analyzer` integration, lazy-load editor admin gate at storefront entry, server-side `revalidate` hints (competes with T1 R8 storage layer), InlineSkeleton adoption across remaining 12 cross-plugin renderers, ConfirmDialog adoption across 29 `confirm()` sites (R1 deferral — primitive ready, mechanical adoption again deferred). |
| 52 | AI page builder plugin (T3 R7) | [prior research/04-plugin-ai-builder.md](prior%20research/04-plugin-ai-builder.md) | T3's Round-7 ships `@aqua/plugin-ai-builder` at `04 the final portal/plugins/ai-builder/` — the AI page builder eds requirements lists as v1-future. `scopePolicy: "either"`, `requires: ["website-editor"]`, `core: false`. Operator types a one-line description; Claude returns a `BlockTree[]` using the website-editor's 58-block library + the 18 cross-plugin block ids T3 R3 registered. **Goal A** — manifest (3 navItems Generate/History/Settings, 4 admin pages, 2 settings groups model+auth, 0 storefront blocks; per-install `anthropicApiKey` on `install.config` mirrors T2's per-install Stripe pattern); `Generation { id, prompt, blockTree, modelId, fallbackModelId?, status, costCents, validationError?, createdBy, createdAt, contextHints? }`; read-only block-schema projection of `BLOCK_REGISTRY` + the 18 cross-plugin ids. **Goal B** — `anthropicClient.ts` (Messages API + prompt caching with `cache_control: ephemeral` on the static block-library system prompt; usage exposes `cacheReadInputTokens` + `cacheCreationInputTokens`), `generationService.ts` (build system prompt → call Anthropic at default model Haiku 4.5 → parse JSON-mode `BlockTree[]` → validate → on validation failure retry once on fallback model Sonnet 4.6 → persist via `GenerationStore` port → metrics counter increments when `cacheReadInputTokens > 0`), 6 API routes (POST /generate, GET /history, GET /history/:id, DELETE /history/:id, GET /metrics, GET /healthcheck). **Goal C** — `components/editor/GenerateModal.tsx` (NEW) thin client over `/api/portal/ai-builder/generate` lives in website-editor (only the editor knows the active page's block array); `EditorTopBar.tsx` gains the ✨ Generate button; `EditorPage.tsx` owns modal lifecycle + `onInsert(tree)` appends to active page's blocks. Streaming SSE preview deferred to R8 (POST + spinner is v1). Smoke `src/__smoke__/ai-builder.test.ts` 3/3 pass via `tsx --test` (mocked-API generate persists+validates / invalid block triggers fallback-model retry / cache-hit counter increments). Website-editor smoke unchanged at **92/92**. tsc clean across both plugins. **Cross-team**: T1 register in `_registry.ts` + mount API routes + inject real GenerationStore (memory v1 → Postgres) + encrypt `install.config.anthropicApiKey` at rest (same treatment as T2's per-install Stripe keys). **R8+ deferred**: streaming SSE preview, image generation for hero blocks, per-agency cost ceilings + circuit breaker, "insert at selection" cursor logic (currently appends), history-deep-link from modal. |
| 50 | Deployment + domains — Round 2 (T6 R2) | [prior research/04-deployment-domains-round2.md](prior%20research/04-deployment-domains-round2.md) | T6's Round-2 makes R1's scaffolding shippable. **Phase A** (commit `2f93a18`): operator deploy runbook at `01 development/runbooks/deploy.md` (444 lines, 10 sections — CLI bootstrap / pre-deploy checklist / env vars / shared-portal deploy / per-client deploy / post-deploy verification / custom-domain runbook / rollback / cron / troubleshooting / glossary). Anything contradicted by reality wins → patch the runbook. **Phase B** (commit `b61f587`): foundation Vercel domain client at `04 the final portal/portal/src/lib/server/vercelDomain.ts` (server-only re-export) + `vercelDomain.impl.ts` (the impl, NO server-only guard so `tsx --test` can drive it). Public API: attachDomain / verifyDomain / removeDomain / configFromEnv / isVercelDomainConfigured / readEnvToken / readEnvTeamId / normaliseHostname. 409 `domain_already_in_use` treated as success. Network errors / non-JSON return typed error results. CLI helper at `scripts/attach-domain.mjs` (pure JS) — `--hostname=<host> [--verify\|--remove]` reads VERCEL_TOKEN/PROJECT_ID/TEAM_ID env, JSON output. Mock-smoke `04 the final portal/portal/scripts/smoke-vercel-domain.test.ts` runs via `npm run smoke:vercel-domain` (tsx + node:test) — 11/11 pass: configFromEnv reads/throws / isConfigured / normaliseHostname / attach POST shape / 409=success / 500-error message / empty-hostname rejected pre-fetch / verify POST shape / remove DELETE shape / network-error handling. Real-creds smoke deferred — no sandbox token in the autonomous loop; manual runbook documented in chapter §3f for Ed to run when sandbox creds available. portal devDeps += `tsx`, `server-only`. Plugin's `vercelClient.ts` (R1) and foundation's `vercelDomain.impl.ts` are parallel copies until foundation wires the workspace dep — dedup tracked as R3 candidate. **Phase C** (commit `14c5b2a`): DNS-verify polling client component for the domains plugin. `DomainStatusBadge.tsx` ("use client") replaces R1's static StatusBadge. SSR-safe initial state from server snapshot; on mount, when status=pending, polls `/verify` every 30s up to 10 polls (5-min window). Stops on verified/error or pollCount=10. Visual states: pending/idle (amber dot) / pending/polling (blue + pulsing dot + "checking… (n/10)" `aria-live=polite`) / pending/exhausted (amber + "DNS not propagated after 300s — click Re-check") / verified (green) / error (red + last-error). DNS records the operator must add surface via `<details>` disclosure when present. DomainsPage.tsx wired to use the new badge; Verify + Remove form actions reshaped via `withQuery` helper to honour the apiBase's optional `?clientId=` query string. tsc-clean standalone; 8/8 plugin-service smoke unchanged. **Phase D** (this commit): chapter `04-deployment-domains-round2.md` (8 sections — what R1 left, runbook, foundation client + CLI + smoke, polling badge, smoke status, cross-team handoffs, where each artefact landed, R3 candidates) + this MASTER row + tasks.md row done. **Cross-team handoffs**: T1 R8 dev rewrites mirror T6 R1 prod rewrites verbatim (no conflict; single-source-of-truth `scripts/stitch-rewrites.json` deferred to R3); T2 R11's export-to-repo generator should optionally call `attachDomain` directly from server code when `customDomain` is configured; T5 R2+'s per-Live-client portals follow the same domain-attach loop after deploy. **R3 candidates**: real-creds smoke against sandbox + bake into runbook, foundation-plugin Vercel-client dedup, single source-of-truth stitch-rewrites file, polling refinements (configurable window, exponential backoff, push-notification webhook), demo cron wiring. |

---

## Discipline

- Update this index **before** you finish any task. If a session ends
  without a chapter row, the learning is lost.
- Don't write speculative chapters — only write what's been verified in
  the codebase or confirmed by the user.
- When a chapter goes stale, mark its row with `(stale — superseded by #NN)`
  rather than deleting. The history of decisions matters.
