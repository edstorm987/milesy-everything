/loop

# T3 — Round 046: Static export — bundle sitemap.xml + robots.txt

R033 shipped static-site export (downloads ZIP). R036+R044 shipped
advanced sitemap+robots. Bundle them: when an operator exports a
static site, the ZIP includes `sitemap.xml` + `robots.txt` generated
from R036 advanced helpers (not the narrower R014 ones).

## Pre-read

- T3 R033 chapter `04-static-export.md` (current export pipeline).
- T3 R036 + R044 chapters (advanced generators + host routes).

## Scope

**A** — Refactor `staticExport.ts`: replace R014 sitemap calls with
R036 `selectSitemapPages` + `buildSitemap` + `buildRobotsTxt`. Use
the same advanced filters (drafts, redirect-sources, private,
no-index).

**B** — Locale-aware: when site has locales, ZIP includes
`sitemap.xml` (default locale) + `sitemap-<locale>.xml` per locale,
each generated by `handleLocaleSitemapXml`-equivalent helper.

**C** — Smoke `§ Static export sitemap bundle` (≥10 — single-locale
bundle has sitemap + robots; multi-locale bundle has all sitemap
files; redirect-sources excluded; drafts excluded).

**D** — Chapter `04-static-export-sitemap-bundle.md` + MASTER row.

## NOT in scope

- Retiring R014 — still used elsewhere; R+1 cleanup post-ship.
- Editor UI for sitemap config (post-ship — operator just picks
  locales on pages).

## When done
DONE referencing `046-static-export-sitemap-bundle.md`.
